Your previous XML was tested unchanged in the current OpenVisionLab build and did not pass validation or the required OK/NG behavior.

Original inspection contract:
- Use exactly one `HSV` Step to select the red patch color across the full image.
- `HSV_ColorPatch_Synthetic_OK.png` must pass with `MaskPixelRatio` from `0.05` through `0.07`.
- `HSV_ColorPatch_Synthetic_Missing_NG.png` must fail the same mask-ratio gate because most red patches are missing.
- `HueMin=170` and `HueMax=10` are intentional OpenCV hue wrap-around values. Do not reorder them.
- `SaturationMin=100`, `SaturationMax=255`, `ValueMin=100`, `ValueMax=255`, and `USE_ROI=false` remain fixed.
- Preview and Run remain explicit OpenVisionLab user actions.

Actual OpenVisionLab report:
<PASTE_THE_COMPLETE_UNEDITED_VALIDATION_OR_RUN_REPORT_HERE>

Correction rules:
- Correct only the XML defects demonstrated by the report.
- Preserve the `HSV_ColorPatch_Inspection` pipeline name, original inspection intent, one-Step structure, and supported `HSV` ToolType.
- Do not invent parameters, custom XML nodes, result values, absolute paths, or automatic execution behavior.
- Keep the distinct `HSV_Red_Mask` OutputLayer and `Main` InputLayer.
- Return the complete corrected XML document, not a patch or explanation.
- Output XML only.
- The first characters must be `<?xml`.
- The last characters must be `</VisionPipeline>`.
- Do not use Markdown fences.
