# GPT Feature Card Direct-Success Evidence

> **AI-generated content disclosure:** `response.xml` was generated by GPT in a user-operated ChatGPT conversation. The OpenVisionLab development workflow manually preserved, validated, imported, and explicitly replayed the response. The exact GPT model/version was not supplied. OpenVisionLab maintainers remain responsible for reviewing this evidence.

## Publication Status

- Status: `GO / ADDED TO THE DEV WORKTREE`
- Repository inclusion: explicitly approved by the user on 2026-07-16 KST
- Package path: `docs/evidence/llm/20260716_feature_card_gpt_direct_success`
- Commit/push status: not committed or pushed as part of this inclusion step yet
- Original repository: not touched

## Classification

- Provider: GPT through a user-operated ChatGPT conversation
- Transfer: manual copy into the OpenVisionLab validation workflow
- Exact model/version: unknown
- API evidence: no
- Evidence type: real constrained direct-success transcript
- Correction rounds: 0
- Human review: completed

The prompt supplied the exact tool family, supported parameters, template path, and acceptance gates. This package proves that one GPT response followed that contract and executed correctly. It does not prove independent algorithm selection from an ambiguous image request or broad provider/model reliability.

## Package

| File | Purpose |
| --- | --- |
| `prompt.md` | Unchanged public-safe first-round prompt |
| `response.xml` | Unchanged GPT response |
| `nominal_result.png` | OpenVisionLab result for the nominal feature-card image |
| `negative_result.png` | OpenVisionLab result for the wrong-card negative image |
| `privacy_review.md` | Privacy, public-asset, metadata, and exclusion review |

The result PNGs were produced by OpenVisionLab, not by GPT.

## Public Replay Assets

| Role | Repository path | SHA-256 |
| --- | --- | --- |
| Nominal OK | `docs/samples/public/Feature_Card_Synthetic_OK.png` | `AA0A0092FFA40A22CCD1C44AF66BF59FC198BD89A39E649918FDD090E9B63C26` |
| Wrong-card NG | `docs/samples/public/Feature_Card_Synthetic_Wrong_NG.png` | `3791EC6701F4426A3DCADAE588F5B0BFAA784990326BFB8C07E66C33CAA3703F` |
| Feature template | `docs/samples/public/templates/Feature_Card_Synthetic_Template.png` | `75D535A584969862B8B1CE600EE7DCB88E5AC04BAD5C4BAF92EC89C3F1D0A1CD` |

All three inputs are OpenVisionLab project-authored public synthetic assets registered by the public sample manifest.

## Integrity

| File | SHA-256 |
| --- | --- |
| `prompt.md` | `B9F32A8C0CBD00C1569C3CC9049ABFF00C928B8F47C6BEFFD5EC64D60CD7BBFE` |
| `response.xml` | `F06E9259E8FE48363142AFFDBB047EC191FCF90B91A19B56CDE7797757E4E245` |
| `nominal_result.png` | `6FD550656825FB95E2133F6532885FEC37BF131B85A70361EC3ED780B84F4914` |
| `negative_result.png` | `FF663DD3AF10A846FC146507DDA3F05A600F13EE8B6A19119F3C8BAC0D4EC57B` |

These four files are byte-identical to the audited artifact-only candidate and preserved raw evidence.

## Sanitization

- Raw validation/run reports, stack traces, runtime recipe paths, generated runtime identifiers, local EXE paths, and session data are excluded.
- Text scanning found no drive path, user-home path, application-data path, Codex attachment path, email address, URL, or credential token.
- Both result PNGs are 572x420 and contain only `IHDR`, `IDAT`, and `IEND` chunks. They contain no textual or EXIF metadata.
- The XML uses only the documented application-startup-relative public template path.

## Reproduce

Run from the repository root. The helper waits for the latest built EXE and verifies the expected process exit code.

```powershell
dotnet build "OpenVisionLab.sln" -c Debug -p:Platform="Any CPU"

$repo = (Resolve-Path .).Path
$exe = (Resolve-Path ".\bin\Debug\OpenVisionLab.exe").Path
$package = Join-Path $repo "docs\evidence\llm\20260716_feature_card_gpt_direct_success"
$evidence = Join-Path $repo "artifacts\feature_card_evidence_replay"

function Invoke-OpenVisionSmoke {
    param(
        [string[]]$Arguments,
        [int]$ExpectedExitCode = 0
    )

    $process = Start-Process -FilePath $exe `
        -ArgumentList $Arguments `
        -WorkingDirectory (Split-Path $exe) `
        -WindowStyle Hidden `
        -Wait `
        -PassThru

    if ($process.ExitCode -ne $ExpectedExitCode) {
        throw "Expected exit code $ExpectedExitCode, got $($process.ExitCode)."
    }
}

Invoke-OpenVisionSmoke -Arguments @(
    "--smoke", "llm-xml-draft-file",
    "--draft", (Join-Path $package "response.xml"),
    "--output", (Join-Path $evidence "validate_import")
)

Invoke-OpenVisionSmoke -Arguments @(
    "--smoke", "llm-xml-image-run",
    "--draft", (Join-Path $package "response.xml"),
    "--image", (Join-Path $repo "docs\samples\public\Feature_Card_Synthetic_OK.png"),
    "--output", (Join-Path $evidence "nominal")
)

Invoke-OpenVisionSmoke -ExpectedExitCode 1 -Arguments @(
    "--smoke", "llm-xml-image-run",
    "--draft", (Join-Path $package "response.xml"),
    "--image", (Join-Path $repo "docs\samples\public\Feature_Card_Synthetic_Wrong_NG.png"),
    "--output", (Join-Path $evidence "negative_expected_ng")
)
```

Expected results:

- Validation/import: PASS; 1 Step; 0 errors; 0 warnings; both template dependencies copied; `ImageRun: SKIPPED`.
- Nominal: PASS; `ResultCount=1`; `ScoreMin/Max/Avg=96.7`; overlay count 1.
- Wrong-card negative: expected product NG; validation remains successful; `ResultCount=1`; `ScoreMin/Max/Avg=26.7`; overlay count 1.
- The negative command returns exit code 1 because the smoke runner represents the correct inspection rejection as a failed image-run command.

Validation/import and image execution remain separate explicit actions. This evidence package does not add automatic Preview/Run, create layers, or change layer-routing behavior.

## Review Record

The inclusion decision, privacy review, public-asset provenance, and tracked-path replay are recorded in [`OPENVISIONLAB_LLM_FEATURE_CARD_PUBLICATION_REVIEW_20260716.md`](../../../OPENVISIONLAB_LLM_FEATURE_CARD_PUBLICATION_REVIEW_20260716.md).
