# OpenVisionLab Recipe XML Handoff 0.1.11 Strict Corpus Preflight

Date: 2026-09-04 (KST)  
Status: `PASS` for candidate-only corpus freeze/preflight; benchmark admission not run

## Scope and identity

The v0.1.11 candidate was evaluated against a new D-drive-only corpus identity:

`openvisionlab-rule-based-round1-v011-strict-corpus-20260904`

Evidence root:

`D:\OpenVisionLab-TestData\OpenVisionLab_Dev\openvisionlab-rule-based-round1-v011-strict-corpus-20260904`

The failed v0.1.10 root was copied without in-place repair and remained immutable:

`D:\OpenVisionLab-TestData\OpenVisionLab_Dev\openvisionlab-rule-based-round1-v010-contract-recovery-20260904`

Its freeze-record SHA-256 remained
`06B66F9C349FD615FB8F9108BF28E0C44D32F3717DBFCACD6825F7308F1C74BD`.

The new corpus is classified `EXPOSED_SELECTION_REGRESSION_ONLY`. The installed
skill remains candidate `0.1.11`, `EXPLICIT_ONLY`, outside normal dispatch,
inactive, and unqualified. Product EXE execution, Import/Preview/Run, Recipe or
layer mutation, Original-repository work, commit/push/release/deployment, runtime
parity, and human comparison were not authorized.

## Bounded corpus repairs

- The retained safe static baseline now uses the strict `Main -> SourceFrame`
  frame identity and its v0.1.7 status/qualification/XML/validation/action tuple
  is preserved.
- S1-04 and S3-01..04 intents were made lossless and pixel/edge-family scoped;
  no calibration, acceptance, dark-band, or unsupported semantic claim was added.
- S4-01..04 now carry one explicit operator-owned lock:
  `FIXTURE_MIN_VALID_PIXEL_RATIO=0.25`, owned by `OPERATOR_LOCK` and linked to the
  frozen operator-lock evidence. The value is a corpus case lock, not a skill
  default.
- Red fixtures retain integrity-first, fail-closed, upstream-WAIT propagation,
  and product-side-effect boundaries. All local path/hash pairs were refreshed;
  adversarial `providedSha256` values remain adversarial inputs.
- Forty-eight copied legacy runner packet templates were quarantined under
  `temp/stale-review-packets-20260904`; the active `runner/review-packets`
  directory has zero files. They are not review evidence and will only be
  regenerated by an explicitly admitted Reviewer phase.

## Frozen artifact identity

| Artifact | SHA-256 |
| --- | --- |
| `frozen/public/public-manifest.json` | `9D5D5745B2C0861EEB389EE17722B23A78AB512E09592F2BF806CB8C19A1A41D` |
| `frozen/private/freeze-record.json` | `0DF579083B7F72C473A2C97B5C875FBE97FEB152101E9615421C4CAC308845D8` |
| `frozen/private/hidden-answer-key.json` | `975F87D58D6C22CE571403D4A2DC337E203CA6A28FB5FEE877347F30AFAF5533` |
| `frozen/private/red-team-design.json` | `4676706B2CFF399EB628DE227FC79C3D2EF3529F9B9C8CEA0714822AF3210163` |

The manifest contains 16 clear cases, 32 red-team cases, and 14 public contract
references. The freeze timestamp is `2026-09-04T05:19:00.379Z`.

## Verification gates

| Gate | Result | Evidence |
| --- | --- | --- |
| Freeze/contract load | `PASS` | `preflight/contract-validation.json` (exit `0`) |
| Harness self-test | `PASS` | `harness/self-test-output/self-test-result.json`, `preflight/harness-self-test.txt` (exit `0`) |
| Direct v0.1.11 handoff validator | `PASS` | `preflight/baseline-direct-validator-output.json` (exit `0`) |
| Baseline-wrapper projection validator | `PASS` | `preflight/baseline-projection-validator-output.json` (exit `0`) |
| Static XML compatibility | `PASS` — 13 XML roots, 1 recipe XML | `preflight/baseline-static-validator-output.txt` (exit `0`) |
| Harness regression suite | `PASS` — 15 tests | `preflight/harness-regression-tests.txt` (exit `0`) |
| Author-runner regression | `PASS` — 1 test | `preflight/runner-regression-tests.txt` (exit `0`) |
| Python compilation | `PASS` — five frozen Python files | `preflight/py-compile.txt` (exit `0`) |
| Prepare | `PASS` — 112 immutable metadata records | `preflight/prepare-exit-code.txt` (exit `0`) |
| Metadata/freeze gate | `PASS` — 112/112 exact records, zero extra attempt files | `preflight/metadata-freeze-gate.json` (exit `0`) |
| Strict corpus integrity | `PASS` — no retired IDs, `SourcePixelFrame`, or extra attempt artifacts | `preflight/strict-corpus-integrity.json` (exit `0`) |
| Combined final gate | `PASS` — all gates and boundaries rechecked together | `preflight/final-gate.json` (exit `0`) |

The static validator was invoked with the required argument order:

```text
RecipeXmlCompatibilityCheck.exe C:\Git\2D\Dev\bin\x64\Release D:\OpenVisionLab-TestData\OpenVisionLab_Dev\openvisionlab-rule-based-round1-v011-strict-corpus-20260904\frozen\public\red-fixtures\safe-static-baseline-bundle\positive-v0.1.7
```

## Closure boundary

This preflight proves that the new candidate corpus is internally frozen,
hash-consistent, and ready for a separately authorized Round 1 admission decision.
It does not prove author quality, reviewer quality, runtime parity, production
robustness, qualification, or skill activation. Authors, Reviewers, audit
admission, scoring, and Original-repository changes were intentionally not run.

Next priority is a separate operator-approved benchmark-admission decision under
this immutable identity, followed by the existing Authors/Reviewers gates only if
that admission is explicitly authorized.
