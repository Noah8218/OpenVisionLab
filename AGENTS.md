# AGENTS.md

This file defines the working agreement for Codex in this repository.

## Work Location

- Primary implementation and verification work starts in `C:\Git\OpenVisionLab_Dev`.
- `C:\Git\OpenVisionLab` is the original OpenVisionLab repository that receives reviewed, stabilized changes from Dev.
- Do not bulk-copy Dev over the original repository. Move changes by reviewed patch, cherry-pick, or import.
- Do not run `git push` unless the user explicitly requests `PUSH`.

## Product Identity

- OpenVisionLab is an OpenCvSharp4-based rule-based vision recipe workbench. Rule-based teaching, execution, drawings, and repeatable validation are the product core.
- LLM support is an optional, maintenance-mode XML authoring aid. It is not the primary product direction and must not be a prerequisite for operating the workbench.
- It is for learning, verifying, and composing image-processing inspection recipes with tools such as Threshold, Blob, Contour, Line/Length, Matching, EdgeBasedMatching, and Feature/Shape-style workflows.
- The main workflow is sample image -> direct PropertyGrid teaching and Pipeline composition -> explicit Preview/Run -> layer/result comparison -> N-sample validation -> saved recipe. Existing LLM XML draft/validation/import may optionally accelerate the composition step.
- It is not a camera, lighting, PLC, or I/O integration platform.

## User-Centered Workflow And Persisted Setup

- Design each feature from the operator's goal and the shortest safe normal
  workflow, not from internal screen, component, class, or storage boundaries.
- When one task requires related settings across several views, dialogs, or
  buttons, treat that friction as a design problem. Consolidate settings that
  belong to one durable workflow into one coherent first-use option or setup
  surface.
- Persist reusable setup only after explicit operator confirmation and at the
  narrowest correct Tool, Recipe, project, workspace, or user scope. Restore
  it on the next equivalent use so the operator does not repeat the same setup.
- Keep restored values visible and editable, provide an explicit reset/default
  path, validate them before reuse, and explain stale or incompatible state.
- Do not silently share task-specific settings across unrelated Recipes,
  projects, workspaces, or users. Do not remember destructive,
  security-sensitive, or safety-critical choices implicitly.
- Restoring configuration must not execute Preview/Run, create/delete/select
  layers, change the active layer, or mutate Pipeline routing.
- Verify reusable setup through save, close/reload/reopen, exact restoration,
  visible reset, stale-state rejection where applicable, and zero unintended
  execution/layer/routing side effects.
- Use
  `docs/reports/OPENVISIONLAB_USER_CENTERED_WORKFLOW_DIRECTION_20260729.md`
  as the reusable admission template and future development direction.

## LLM Maintenance Mode And Preserved Skill Evidence

- P196 records the user's decision to freeze LLM expansion and move development priority to the deterministic rule-based workbench. Do not start new provider integrations, consumer-web automation, prompt families, inspection-intent skills, or repeated LLM correction campaigns unless the user explicitly reopens this direction.
- Preserve the existing LLM Assistant, Guided Setup contracts, XML guide/catalog, validation/import gates, P168 Pin Phase 2 evidence, and P195 hybrid Phase 1 workflow. Fix regressions, data-loss risks, invalid XML acceptance, or broken compatibility when concrete evidence exposes them; otherwise do not spend priority cycles polishing or expanding LLM features.
- The frozen P169 Test and the missing natural GPT failure remain historical prerequisites, not active backlog. Do not solicit, manufacture, or wait on new GPT/Gemini/Claude transcripts during rule-based development.
- Reopening planned LLM development requires an explicit user decision after the non-LLM fixture/relative-ROI workflow is usable, the selected rule-based metrology family has stable drawings/metrics and N-sample evidence, and the deterministic XML/tool contract is stable enough that LLM work composes proven capabilities rather than compensating for missing algorithms.
- Provider-specific browser UI has short shelf life. Manual prompt copy/XML paste may remain available, but consumer-web automation and API credentials must not become product dependencies.
- P197 completes one bounded non-LLM fixture/reference-ROI operator path without adding a new algorithm or LLM surface. The public `Matching -> RotateScale NormalizeImage -> Threshold -> Blob` pipeline passes its translated synthetic Good sample at `FixtureValidPixelRatio=0.748` and `ResultCount=1`, while the paired missing-pad sample reaches the same fixed reference ROI and fails there at `ResultCount=0`. Treat this as workflow evidence for one public pair, not rotation/scale, unseen-data, calibration, or field-robustness evidence. Evidence: `artifacts\p197_rule_based_fixture_workflow_20260722`.
- P198 audits the existing general `LineDistance` and specialized `GapEdgePair` paths without adding a new algorithm family. General LineDistance already supports independent Line A/B ROI, polarity, scan direction/angle, fitted-edge distance, pixel/mm results, and distance distribution; the dark-band Gap selector remains intentionally limited to one reviewed long near-horizontal dark band. The only dataset-proven defect was drawing fidelity: distinct A/B ROIs were both used at runtime but only A was retained. Current runtime now draws one `Measurement ROI` when they are equal and separate `Line A ROI` / `Line B ROI` overlays when they differ. The frozen public OK measurement remained `37.014 px` average / `1.999 px` range, the Wide-Pin comparison measured `18.300 px` / `3.994 px` with the same XML, and the P189 Gap canonical remained `14.4 px` / range `2`. Evidence: `artifacts\p198_line_caliper_audit_20260722`. This is drawing and focused regression evidence, not industrial semantic correctness or calibration proof.
- P199 closes the Recipe Manager selected-Step PropertyGrid Line A/B persistence defect. A frozen asymmetric `LineDistance` Step reproduced no-edit apply loss of Right ROI, polarity, vertical projection, and manual-angle values. Recipe Manager now exposes explicitly labelled A/B fields for those settings, preserves unrepresented per-line parameters from their loaded baselines, and still applies a changed compact shared field to both lines. Direct mapper edit, actual Recipe Manager apply/save/reload, zero-Preview/Run, focused UI, related PropertyGrid/Line Tool regressions, and the unchanged P198 runtime measurement passed. Evidence: `artifacts\p199_line_pair_property_roundtrip_20260722`. This proves edit/persistence fidelity for the named contract, not general Caliper semantic accuracy.
- P200 freezes a four-case `LineDistance` Caliper orientation/polarity matrix without runtime changes. One public synthetic rightmost-pin-to-datum clearance is replayed as horizontal/vertical and bright/dark variants using transformed ROIs and the actual X/Y projection-direction contract. All four final cases returned `ResultCount=22`, `DistancePxAvg=37.000..37.014`, `DistancePxRange=1.999..2.000`, and current-run A/B ROI/edge/distance drawings that select the same transformed physical boundaries. Evidence: `artifacts\p200_line_caliper_matrix_20260722`. This is one synthetic configuration matrix, not adjacent pin-gap, center-pitch, calibration, unseen-data, or field-robustness evidence.
- P201 adds a separately named pixel-only `CenterPitch` mode to the existing `PinArrayGap` family without changing legacy XML: missing `MeasurementMode` remains `EdgeGap`. Center pitch uses adjacent detected dark-pin centers and publishes only `PitchCount` plus `PitchPxMin/Max/Avg/Range`; it does not relabel edge-clearance metrics or publish calibrated pitch. Recipe Manager selected-Step PropertyGrid now edits and round-trips the mode, row ROI, and dark-pin detection fields while preserving unrepresented parameters and zero Preview/Run side effects. A frozen three-case semantic matrix proved uniform and varied-width rows both at `PitchPxAvg=60`, `PitchPxRange=0`, while one shifted center produced `PitchPxRange=12` and failed the frozen `<=2` gate; exact current-run center/pitch drawings are retained. Evidence: `artifacts\p201_pin_center_pitch_20260722`. This is synthetic dark-pin/single-row evidence only, not bright polarity, calibrated units, non-tuned N-sample, unseen-data, or field-robustness proof. The frozen LLM Pin Guided Setup v1 remains adjacent EdgeGap-only.
- P202 completes the first direct non-LLM CenterPitch N-sample slice without opening the reserved P169 Test. P170 Working Train/Validation audited at 252/252 present, unique, and hash-matched rows. One P168-derived two-row XML froze `PitchPxRange <= 12` from Train, then accepted Good 178/178 and rejected `pitch_error` 26/26 on Train and accepted 36/36 / rejected 12/12 on Validation with zero runtime errors or misclassifications. All 252 drawing hashes verified and all 44 deterministic queue drawings were opened; centers and adjacent pitch lines stayed on the intended dark pins. The batch evidence tool now retains combined overlays for every executed Step instead of only the final result image; production runtime was unchanged. Decision: `Keep with documented limits`. Evidence: `artifacts\p202_pin_center_pitch_nsamples_20260722`. This is one previously observed synthetic/augmented corpus, pixel-only and dark-pin-only; it is not independent blind Test, calibration, real-production, or field-robustness proof. Do not consume the P169 Test for this non-LLM track.
- P203 proves the frozen P202 Working Validation candidate through the existing Recipe Manager Validation Set and saved Run History workflow without a manifest importer or product runtime change. A task-local, hash-verified staging copy registered exactly 36 OK and 12 NG rows; the product returned 36 correct accepts and 12 correct rejects with zero errors or misclassifications. The UI retained both `PitchPxRange <= 12` gates, two stored row drawings, exact `PitchPx*` metrics, and a 24-row deterministic review queue under SHA-256 `196A8EF87728A867F4542F0A09D0AEEFB9C803E6041544893EB089770589E21F`. Queue filtering preserved Preview/Run count, layers, and routing. No product integration loss was reproduced, so stop CenterPitch tuning on this corpus. Evidence: `artifacts\p203_center_pitch_product_workflow_20260722`. This completes product-workflow integration only; the P202 independent-data, polarity, calibration, production-variation, and field-qualification limits remain.
- P204 audits the operator-provided `Pins_500_OK_NG\Pins` corpus for a separate missing-pin/count intent without changing product code. The global class map and per-row YOLO labels isolate exactly 50 `Pins:missing_pin` rows as class 30 from the other 200 NG rows; all 250 OK labels/masks are empty/zero, all 50 target labels have one non-empty mask fully covered by the supplied box, and all 300 selected image hashes are unique. The supplied split is Train 178 OK/38 target, Validation 36/12, and Test 36/0, so the existing Test cannot qualify missing-pin rejection. Select the existing `Threshold -> Blob` ResultCount path for the first semantic matrix because the corpus contains bright connected pins; do not reuse dark-run `PinArrayGap` or add a new family. Contour remains a fallback only if Blob component filtering/drawings fail. Evidence: `artifacts\p204_missing_pin_count_audit_20260722`.
- P205 rejects the first one-fixed-raw-ROI `Threshold -> Blob` missing-pin candidate after actual drawing review. The ROI `0,40,768,360` and first `150 / area 200..5000` probe produced 11 on three OK rows, 10 on two missing rows, and 12 on `Pins_NG_0001` because lower rail fragments entered the Blob set. One bounded `area 1700..3000 / expected count 11` correction numerically accepted all three OK and rejected all three missing rows, but `Pins_NG_0001` still counted a horizontal rail fragment and missed the right border pin, making its count of 10 coincidental. Stop raw ROI/threshold/area tuning and do not call numerical 6/6 semantic success. The batch evidence harness now clones the source before execution so ROI/component drawings appear on the original image; product Blob runtime is unchanged. Evidence: `artifacts\p205_missing_pin_blob_semantic_matrix_20260722`.
- P206 completes one bounded fixed-rectification candidate on the exact P205 six rows without changing product runtime. Existing `RotateScale` at `+10 deg` straightens the shared row; Binary threshold 150 and Blob area `200..5000` inspect aligned ROI `40,140,660,158`, intentionally excluding source-border-truncated pins and judging nine stable interior slots. Two geometry-only correction cycles first removed rail fragments and then excluded unstable border crops; final raw-source three-Step replay accepted OK 3/3 at count 9 and rejected missing 3/3 at count 8. All six aligned-stage current-run drawings contain physical pins only, and source/aligned/XML hashes are retained. Evidence: `artifacts\p206_missing_pin_rectified_blob_matrix_20260722`. This is six-row shared-pose/interior-slot evidence only, not pose-extreme, full Train/Validation, independent Test, unseen-data, lighting, or field-robustness proof. Do not tune this candidate per image; freeze it for the next extreme matrix.
- P207 rejects that exact frozen P206 candidate on a pre-execution, source-geometry/hash-selected eight-row pose/border matrix. Four OK and four supplied-label-inside-ROI missing rows were selected by per-role pin-base fit-angle and aligned left/right-margin extremes before reading candidate outcomes. Six rows were semantically correct, but `Pins_OK_0243` admitted a right boundary pin and false-rejected at count 10, while `Pins_NG_0111` admitted the same boundary pin and offset a true interior missing pin, false-accepting at count 9. All eight current-run drawings were opened and hashes retained. Do not tune or run Train/Validation from this candidate. Evidence: `artifacts\p207_missing_pin_pose_border_extremes_20260722`. The next bounded option is an operator-reviewable stable-locator proposal before any existing `Matching -> NormalizeImage -> Threshold -> Blob` audit; if no locator outside the variable judged slots is stable, reject this intent for the current tool set.
- P208 shows three non-runtime locator proposals on the exact P207 eight-row matrix without running Matching. The long carrier rail is secondary angle/Y support only because it is horizontally repetitive. The carrier seam/corner is rejected as a sole locator because it is clipped and absent in two rows. The central asymmetric curved machining mark on the lower rail is outside the judged pins and visually present in all eight crops, but remains an operator-review candidate until the user confirms it is the same durable physical feature rather than glare, dirt, or changing surface texture. Do not execute or tune Matching from P208 before that confirmation. Evidence: `artifacts\p208_missing_pin_locator_proposals_20260722`.
- P209 rejects the user-approved Candidate B hybrid on the exact P207 eight rows without product changes. Full-image Matching failed the frozen `ScoreMargin >= 10` gate on all eight because a second rail location scored similarly. One bounded correction applied only the pre-approved Candidate B box union as coarse search ROI `220,350,260,220`; ambiguity then cleared, but `Pins_OK_0203` and `Pins_OK_0243` false-rejected at the unchanged `SCORE_MIN=0.8`, and `Pins_NG_0186` false-accepted because eight physical pins plus one lower rail fragment produced count 9. Operational classification was 5/8. All drawings were opened and retained. Do not lower gates, retune the pin ROI/area/count, run Train/Validation, or reopen this intent without changed physical evidence such as a durable fiducial, different framing, or a separately approved segmentation/tool contract. Evidence: `artifacts\p209_missing_pin_hybrid_locator_audit_20260722`.
- P210 records the user's decision that repeated inspection campaigns are complete as an active development track. Do not run more image batches, switch datasets, tune recipe parameters, or resume LLM XML/provider validation until the user explicitly requests a named inspection validation. The static commercial comparison selected UI/tool gaps instead: first an Object Results Inspector for per-object Blob/Contour rows and click-to-highlight evidence, then a unified Fixture/relative-ROI designer, then a reusable geometric measurement workspace. Audit: `docs\OPENVISIONLAB_RULE_BASED_UI_GAP_AUDIT_20260723.md`.
- P211 completes the first Object Results Inspector slice in Pipeline Review. Explicit Run Review now retains stable Blob/Contour object rows with number, accepted/rejected state, area, center, bounds, angle, and area-filter reject reason; the table and result drawing select each other without another Preview/Run or layer/routing mutation, and saved reports round-trip object rows. Blob retains all area candidates from the shared processing contract. Contour intentionally retains accepted results plus near-filter candidates at or above 25% of `MIN_AREA` so pixel-noise contours cannot flood or stall the UI; this is not an exhaustive list of every sub-threshold contour. Focused current-build Blob and Contour UI smokes and report persistence passed. Evidence: `artifacts\p211_object_results_inspector_20260723`.
- P212 completes the bounded Fixture/relative-ROI UI consolidation in Pipeline Review. One existing named `Matching producer -> NormalizeImage consumer -> reachable downstream CvROI Step` chain now shows its template/search ROI, reference pose/image size, current pose/score/same-template margin/valid pixels, and the same saved ROI as a transformed source polygon plus normalized reference rectangle. Explicit reference teach, producer edit, measurement-ROI edit, and Run Review reuse existing workflows; tab selection does not run, change layers, or change routing. This adds no locator or measurement algorithm and does not qualify the recipe. Evidence: `artifacts\p212_fixture_relative_roi_designer_20260723`.
- P213 completes the bounded General Geometric Measurement Workspace. Successful existing `Line` Steps publish same-run `Segment`/`Start`/`End`/`Midpoint` features; new radial-caliper `CircleGauge` publishes `Circle`/`Center`; and `GeometryMeasure` consumes exact earlier `SourceStep + FeatureName` identities in seven frozen pixel-only modes. Recipe Manager exposes compatible sources through PropertyGrid dropdowns, Pipeline Review retains a read-only Geometry Review with table/drawing two-way selection, and direct/recipe Run Reports preserve typed provenance. Runtime and validator fail closed on missing/failed/NG/ambiguous/wrong-kind/cross-frame/non-finite/out-of-image/degenerate sources and bounded geometry gates. Full/focused builds, all seven math modes, Circle success/reject cases, report round trips, current-source UI captures, and legacy Line/LineDistance/LineIntersection regressions passed. Evidence: `artifacts\p213_general_geometric_measurement_workspace_20260723`. This is pixel-only synthetic algorithm/UI evidence, not calibration, industrial semantic accuracy, unseen robustness, field qualification, or permission to use experimental `OuterCornerIntersection` as a default source.
- P214 completes bounded two-point uniform-scale teaching in Pipeline Review. The operator selects two distinct P213 Point results from one explicit Run and coordinate layer, enters a certified distance in mm/µm/inch, reviews the A/B drawing and pixel distance, and saves exact point/image SHA-256 evidence before explicitly applying the derived millimeters-per-pixel value to one selected compatible measurement Step. Compatibility preserves the legacy XML key `PIXELPERMM`, whose runtime meaning is mm per pixel; LineDistance also receives its existing Left/Right values. Calculate/apply do not run the pipeline or mutate layers/routing. Changed image content, cross-frame points, coincident/same identities, invalid distance, incompatible Step, and input-layer mismatch fail closed. P213 distance/clearance and circle radius/diameter publish mm metrics only with a positive applied scale. Evidence: `artifacts\p214_two_point_scale_teaching_20260723` and `docs\OPENVISIONLAB_TWO_POINT_SCALE_TEACHING_CONTRACT.md`. This is one uniform image-plane scale and synthetic/UI integration evidence, not lens/camera calibration, distortion/perspective correction, non-uniform scale, certified metrology, or field qualification.
- P215 completes the static post-P214 commercial UI/tool reassessment without executing images or reopening LLM work. Current source defines 23 canonical tool families and 42 accepted ToolType names/aliases. P211 already exposes Blob/Contour bounds in stable object rows, and Pipeline acceptance can judge aggregate metrics such as `BoundsWidthMax`; however, `BlobProperty`, `ContourProperty`, the Pipeline mapper/factory, and per-object runtime acceptance remain area-only. The only selected next slice is optional per-object bounding-width/height gates with exact reject reasons and backward-compatible missing-key behavior. Defer angle/aspect/circularity/holes/gray features, easyTouch-style automatic suggestions, navigation rewrites, OCR/barcode, region algebra, new graph engines, datasets, and LLM campaigns until a concrete approved need exists. Audit: `docs\OPENVISIONLAB_RULE_BASED_UI_GAP_AUDIT_20260723.md`.
- P216 completes that bounded Blob/Contour object-dimension slice. PropertyGrid and Pipeline/XML now expose `MIN_WIDTH`, `MAX_WIDTH`, `MIN_HEIGHT`, and `MAX_HEIGHT` as axis-aligned pixel bounding-box filters; absent keys restore `0..1000000` and preserve legacy area-only results. Filtering occurs before `ResultCount`, aggregate area/angle/bounds metrics, accepted drawings, and acceptance evaluation. P211 rows and saved Run History retain rejected candidates with exact width/height reasons. One deterministic five-shape matrix passed for both Blob and Contour with one accepted object and four named dimension rejects; legacy missing-key runs retained all five. Current-build Blob/Contour/Pipeline Review UI smokes and zero-Preview/Run PropertyGrid evidence passed. Evidence: `artifacts\p216_object_dimension_filters_20260723`. This is axis-aligned pixel geometry and synthetic/UI compatibility evidence, not rotated-size, aspect/circularity, semantic classification, operator-dataset, industrial robustness, or field qualification.
- P217 closes proactive deterministic feature expansion after a static post-P216 workflow reassessment. Current source connects PropertyGrid teaching and selected-Step edit handoff to explicit Run Review, Object Results, Fixture/relative-ROI, Geometry Review, Scale Calibration, recipe round trip, saved per-Step drawings/object rows, batch history, and the deterministic review queue. The P210 commercial shortlist is complete through P216, and no remaining candidate has a named operator task plus a reproducible current blocker. Do not select angle/aspect/circularity/holes/gray features, automatic suggestions, navigation rewrites, OCR/barcode, region algebra, or another algorithm merely because commercial products contain them. The next implementation trigger is a concrete operator-blocking workflow or verified regression with current-source evidence; until then there is no active feature priority. Evidence: `artifacts\p217_post_p216_workflow_reassessment_20260723` and `docs\OPENVISIONLAB_RULE_BASED_UI_GAP_AUDIT_20260723.md`.
- P218 implements the explicitly requested deterministic three-point 2D `AffineTransform` family in `C:\Git\Library-Noah` and consumes the separately built DLL from OpenVisionLab. Library-Noah owns point/output/gate validation, `GetAffineTransform`/`WarpAffine`, matrix/decomposition/triangle/valid-pixel metrics, stable fail-closed errors, and destination-point/triangle/transformed-frame drawings. OpenVisionLab adds a PropertyGrid Tool View with explicit Preview only, canonical/alias Pipeline/XML round trip, strict validation, result review, public sample, and Geometry Learn path. The exact source/vendored/build-output `Lib.OpenCV.dll` is assembly `2.1.0.0`, file `2.8.0.0`, SHA-256 `B128CA282C0CD02C36F5CCF0C78C69C6F4834C3376158E8667EEAA7DE494A08B`. Evidence: `artifacts\p218_affine_transform_v1_20260723` and `docs\OPENVISIONLAB_AFFINE_TRANSFORM_V1_CONTRACT.md`. This is known-matrix synthetic integration evidence only; do not infer automatic correspondence, homography, calibration, industrial accuracy, unseen robustness, or field qualification. P218 does not reopen LLM expansion.
- P219 connects three earlier accepted typed `Point` results to the existing Library-Noah `AffineTransform` source coordinates. `Matching` publishes `Center` only when exactly one usable result exists; Line, CircleGauge, and GeometryMeasure Point outputs remain compatible. Recipe Manager exposes ordered Point pickers, preserves fixed numeric source points as the default, and fails closed on missing/duplicate/failed/ambiguous/wrong-kind/cross-frame/non-finite/out-of-image sources. The representative `Matching x3 -> AffineTransform -> Threshold -> fixed-ROI Blob` path retained the unchanged ROI and found one normalized target; duplicate references failed definition validation and direct execution. Evidence: `artifacts\p219_dynamic_affine_fixture_20260723` and `docs\OPENVISIONLAB_AFFINE_DETECTED_POINT_FIXTURE_CONTRACT.md`. This is deterministic same-run wiring and synthetic integration evidence, not automatic correspondence, locator qualification, homography, calibration, unseen robustness, or field qualification.
- P220 runs the operator-approved `R`/`5`/expiry-mark Matching centers through P219 on a preselected 12-row `card_original` pilot. The first fixed run passed 8/12; one geometry-only search-ROI correction removed two `5` omissions and one `P`-for-`R` wrong locator without changing score/angle/scale/Affine/residual gates. r2 produced all three Points and Affine output on 12/12, but only 10/12 met the frozen normalized-center residual `<=3 px`; `OK_0051` was `5.00 px` and `NG_0150` was `4.12 px`. Status is `Incomplete at <=3 px`. Do not lower the gate, run all 500, add Homography, or switch features until the downstream fixed ROI/inspection and allowable registration error are supplied. Evidence: `artifacts\p220_affine_fixture_point_candidates_20260723` and `docs\OPENVISIONLAB_CARD_AFFINE_PILOT_20260723.md`.
- P222 implements the explicitly requested Library-Noah `AutoMPointTool` core as a training-time fixed-size matching-candidate suggestion engine. It ranks deterministic contrast/edge-distribution windows, overlap-suppresses finalists, and reuses `EdgeBasedTemplateMatchingTool` for self-location, strongest-alternative uniqueness, three known synthetic replays, pose error, runtime, drawings, and stable reject reasons. Library-Noah Release build and 60/60 smoke passed; a unique pattern ranked first, identical patterns failed uniqueness, invalid definitions failed closed, and repeat ranking/drawing was identical. It is not a Pipeline Step and must not auto-save a template, mutate a recipe, change layers/routing, Preview, or Run. Evidence: `C:\Git\Library-Noah\artifacts\auto_mpoint_v1_20260724` and `docs\OPENVISIONLAB_AUTO_MPOINT_V1_CONTRACT.md`. P223 completes the OpenVisionLab UI/DLL integration; production qualification remains pending.
- P223 integrates P222 into the existing Edge Based Matching Tool View with PropertyGrid settings, explicit `Analyze candidates`, `Suggested` candidate rows/drawing, and explicit `Use this pattern`. Property edits, analysis, selection, and apply preserve Preview/Run count, layers, active layer, and routes. The source/vendored/current-build DLL SHA-256 is `3D7A0B5D392B096DB3C14091D08E52BBB840772C1BDD1B30BEB15475ABAE28D9`. The operator-provided GPT Pro review was checked against current source and official HALCON/Cognex documentation: keep the current matcher, but its ambiguity diagnostics are not yet fail-closed acceptance. The next bounded matcher slice is an opt-in unique-match contract with internal Top-K and external `Success`/`NoMatch`/`Ambiguous`; only after fixed-ROI evidence may joint pose refinement be considered. Do not start adaptive pattern growth, ODB/CAD, global anchors, Homography, or commercial-parity claims from P223. Evidence: `artifacts\p223_auto_mpoint_ui_20260724` and `docs\OPENVISIONLAB_AUTO_MPOINT_V1_CONTRACT.md`.
- P224 completes that opt-in edge-matcher unique-result contract. Missing XML keys preserve legacy behavior (`false`, margin `0.03`); enabled mode requires `NUM_MATCH=1`, one search region, and a finite normalized margin. Library-Noah retains at least eight internal candidates independently of the external one-result request, returns exactly one result only for `Success`, and returns none for `MatchingNoResult` or `MatchingAmbiguous` with normalized state/margin/alternative metrics and an exact reason. OpenVisionLab exposes the two PropertyGrid/XML fields without automatic Preview, validates the one-result/one-region contract, and preserves the runtime reason in review diagnostics. Library-Noah Release build and 64/64 smoke passed on legacy, distinct-success, repeated-ambiguous, and no-match cases. Source, vendored, and Debug `Lib.OpenCV.dll` are assembly `2.1.0.0`, file `2.8.0.0`, SHA-256 `000C75A7D0E796E166DF6F24C95F264FC001927881B1ED7DE7BAE31913099F6D`. This is bounded synthetic/runtime/UI evidence, not template, ROI, pose, or field qualification. Evidence: `artifacts\p224_unique_match_runtime_20260724` and `docs\OPENVISIONLAB_EDGE_BASED_UNIQUE_MATCH_V1_CONTRACT.md`.
- P225 completes, and rejects, the first fixed-ROI `EdgeBasedMatching` qualification candidate without parameter tuning. The operator-approved P220/P221 card `R` template, 12 hash-frozen rows, angle `-8..8`, scale `0.9..1.1`, and `<=5 px` prior-center gate were replayed as reviewed-ROI unique, broad-ROI legacy, and broad-ROI unique modes. Reviewed-ROI unique produced `0/12` correct accepts, two baseline mismatches, two ambiguity rejects, and eight no-match rejects; broad unique retained the same two wrong accepts as broad legacy. Drawing review proved one high-score/high-margin result selected the `T` glyph rather than `R`, so uniqueness cannot substitute for physical-feature identity. The candidate is `Reject`; do not lower the score, retune per image, run a larger set, or start joint pose refinement from it. OpenVisionLab now preserves existing EdgeBased scale/refinement Pipeline parameters and publishes a successful single EdgeBased result as typed `Center`, enabling later Affine/relative-ROI use only after a different operator-approved candidate qualifies. Evidence: `artifacts\p225_edge_unique_card_r_matrix_20260724`.
- P226 runs the existing Library-Noah Auto MPoint engine once on five diverse public `Sample\EasyMatch` reference images with the current product defaults and no result-dependent tuning. All five current-run drawings and 40 exact-finalist rows were retained; 28 candidates passed the internal gates, 20 were exposed as the capped top-five suggestions, and four of five images produced suggestions. `Frame 1.tif` correctly rejected all eight finalists at the `0.05` uniqueness gate, while `Floppies.jpg` still suggested repeated disk hubs whose fixed orientations made them numerically distinct. This proves the shortlist is useful for operator review but cannot infer durable physical-feature identity. No suggestion was applied and no cross-image Matching run occurred. Evidence: `artifacts\p226_auto_mpoint_easymatch_candidates_20260724_r2`. The next action remains operator selection of one named sample/rank/ROI and confirmation that it represents the same durable feature across representative images; do not auto-select from score alone.
- P227 audits the six operator-provided 500-image EasyMatch corpora as 3,000 hash-verified `all_images` rows and 16 separate `source_file` strata. The first product-path pilot exposed a real contract defect: Auto MPoint verified grayscale candidates while new `EdgeBasedMatchingProperty` instances inherited `USE_THRESHOLD=true / THRESHOLD=0`, erasing usable template edges in 88/104 runs. New EdgeBasedMatching and explicit Auto MPoint apply now default to grayscale-edge matching while XML that explicitly says `USE_THRESHOLD=true` remains compatible. The corrected frozen pilot generated suggestions for 13/16 strata, executed 104 deterministic OK/NG rows with zero runtime/integrity errors, and mechanically advanced 12 strata. Review of every contact sheet stopped Frame 1/2/3 (no suggestion), Frame 4 (2/4 OK), Die1 (repeated grid intersection), and Die2 (image-frame boundary); ten strata remain only `expansion candidates`, not qualified locators. Evidence and the operator-readable report: `artifacts\p227_auto_mpoint_six_corpus_pilot_20260724_r4\OPENVISIONLAB_AUTO_MPOINT_SIX_CORPUS_REPORT.md`. Do not run all 500 rows, auto-apply a candidate, or call any candidate qualified until the operator approves one named stratum from that report.
- P228 adds a reusable self-contained HTML export to the P227 six-corpus command instead of asking the operator to review Markdown. The export embeds the candidate overview and all 13 available result contact sheets as data URIs, includes the full per-stratum decision table and Korean review reasons, links the companion CSV evidence, and exposes a browser `인쇄 / PDF 저장` action without external dependencies. A fresh 104-row replay reproduced P227 with zero logical result delta, zero runtime/integrity errors, 14 embedded images, and zero missing companion links; current Chrome rendering was opened and checked. Primary review document: `artifacts\p227_auto_mpoint_six_corpus_pilot_20260724_r5_html\OPENVISIONLAB_AUTO_MPOINT_SIX_CORPUS_REPORT.html`. This is a report/export and evidence-presentation improvement only; it does not qualify any locator or authorize a 500-row replay.
- P229 adds optional representative-image evaluation to Library-Noah AutoMPoint and the existing OpenVisionLab teaching panel. The legacy one-image API remains unchanged. When representative images are supplied, accepted finalists are replayed with the existing edge matcher and ranked by representative success rate, minimum uniqueness margin, mean score, then original score. The UI marks and selects rank one as `BEST`, while apply remains explicit and analysis preserves Preview/Run count, layers, active layer, and routing. A deterministic `Die Pad 1.bmp` split automatically selected ROI `128,256,96,96`, the same ROI retained by P227, and replayed 4 OK + 4 NG representative plus disjoint 4 OK + 4 NG held-out rows at 8/8 and 8/8 with zero runtime/integrity errors. Library-Noah Release build and 66/66 smoke passed; source/vendored/Debug `Lib.OpenCV.dll` SHA-256 is `B456BE7AFC002BA1535A5892092B746FB44560300961BD71342AAC0E7741B180`. Evidence: `C:\Git\Library-Noah\artifacts\auto_mpoint_representative_v2_20260724` and `artifacts\p229_auto_mpoint_representative_best_20260724`. This is one synthetic/augmented same-source numerical-selection pilot, not semantic identity, all-500, real-capture, production, or field qualification. Do not auto-apply or start a full-stratum replay before operator review of the P229 HTML drawings.
- P230 freezes the operator-approved P229 ROI `128,256,96,96` and replays it without tuning on all 122 `Die Pad 1.bmp` rows: 62/62 OK and 60/60 NG matched, with zero ambiguous/no-match/runtime/integrity failures and 122/122 current-run drawings. A deterministic 35-row decision queue and all nine supplied-defect-mask overlap drawings were opened; every green runtime result stayed on the same central pad/trace feature. Because those nine NG masks intersect the 96x96 template bounds, the locator decision is `Keep with documented limits`, not field-qualified. Evidence: `artifacts\p230_auto_mpoint_die_pad_1_full_stratum_20260724_r2`. This completes this source stratum only; do not rerun or retune it, reuse its template for the distinct Die Pad 2-4 source strata, or call the four-stratum 500-image package qualified.
- P231 closes the product-UI evidence-export gap without changing AutoMPoint or matcher logic. After representative-image analysis, the selected candidate now enables an explicit `Save N-image report` action that writes one self-contained HTML file from the already-retained results. It contains source/template identity, the complete N-row metric/hash table, and a bounded deterministic drawing queue of all failures plus score/uniqueness/runtime/angle/scale extremes and SHA-256 spread rows; N <= 24 retains every drawing. Export fails closed on changed source/settings/files and does not rerun matching, apply a pattern, Preview/Run, or mutate layers/routing. Current-source UI and HTML rendering passed with three rows, four embedded images, zero external image links, and unchanged workspace state; a separate 30-row export retained all table rows with 18 bounded review drawings plus the template. Evidence: `artifacts\p231_auto_mpoint_operator_html_report_20260724\after_current_build_r3`. This is representative-image locator evidence only, not Recipe Manager OK/NG classification or field qualification.
- P232 audits the general N-image product path and freezes a design without changing runtime or UI. Recipe Manager Local Validation Sets already support up to 5,000 registered images, explicit sequential execution, per-image reports/drawings, XML/TSV batch summaries, Run History analytics, and a deterministic review queue. Individual Tool Views remain one-image teaching surfaces; AutoMPoint is the only specialized Tool View N-image report. The selected design adds one capability-driven `N-image verification` entry to the shared Tool View shell, creates a frozen transient one-Step Pipeline through the existing `Add Pipeline` Step adapter, and opens one shared sequential verification/report window. Thirteen current single-input Tool Views are eligible; Arithmetic requires an A/B pairing decision, HSV/Histogram lack current native Step adapters, and Pipeline-only tools remain in Recipe Manager. Do not claim current parallel execution: existing loops are sequential, and bounded `1/2/4` workers require a separate isolation/thread-safety/equivalence audit. Design: `docs\OPENVISIONLAB_TOOL_VIEW_N_IMAGE_VERIFICATION_DESIGN.md`.
- P233 completes P232 Phase 1 as one shared sequential Tool View `N-image verification` surface. Thirteen current single-input Step-adapter Tool Views expose the common action; Arithmetic, HSV, Histogram, AutoMPoint, and Pipeline-only families retain their separate contracts. The modal window accepts multiple files or one top-level folder up to 5,000 images, freezes the current Step exactly once on explicit Run, uses the Tool View's channel-normalization policy while retaining the original source snapshot, executes rows sequentially with stop-after-current behavior, and retains per-image reports, metrics, source SHA-256, result drawings, XML/TSV batch summary, Pipeline snapshot, and deterministic review queue. Self-contained HTML is built only from retained evidence and never reruns the tool. Threshold, Blob, Line, Matching, EdgeBasedMatching, and AffineTransform each passed 30/30 with exact direct-run status/metric equivalence; opening/closing the UI preserved Preview/Run count, layers, active layer, and routing. This is quick execution verification only: no expected OK/NG role or accuracy is inferred, no parallel worker is implemented, and formal labelled validation remains Recipe Manager Validation Set/Run History. Evidence: `artifacts\p233_tool_view_n_image_verification_20260724` and `docs\OPENVISIONLAB_TOOL_VIEW_N_IMAGE_VERIFICATION_DESIGN.md`.
- P234 completes the first bounded real-folder acceptance of P233 without changing the locator or product runtime. The exact frozen P230 `Die Pad 1` EdgeBasedMatching Step and its SHA-256 `7CEAEC5D50259ED1337AB912F0F0A63C673F4A74E692DCDEA01BAA14FC25658F` ran through the same top-level-folder registration and shared N-image service on a deterministic hash-spread 12 OK + 12 NG copy. Folder registration, once-only Step creation, execution, and retained drawings all passed 24/24; report source SHA-256 and decoded pixels matched, and `ScoreMax` reproduced P230 within `0.068` percentage points (gate `<=0.1`). Visual review of the minimum-score `83.76%` row and maximum-delta row kept the rotated box and center on the approved central pad/trace feature. Evidence: `artifacts\p234_tool_n_image_real_folder_acceptance_20260724`. This proves real folder-to-report integration for one frozen locator, not automatic OK/NG classification, new semantic qualification, other strata, parallel execution, or field robustness. Do not repeat this corpus or retune the locator unless its source, configuration, or evidence validity changes.
- P235 completes the explicitly requested bounded handoff from a successful locator Tool View N-image session into Recipe Manager. `Matching`, `EdgeBasedMatching`, and `FeatureMatching` sessions expose one explicit `Promote locator set` action only after every retained row succeeded. Promotion saves the exact one-Step Pipeline text/name, Step SHA-256, template/dependency SHA-256, ordered source-file SHA-256, and image-set SHA-256; every row is `Expected OK` for locator execution only, never inherited defect truth. Hash-locked sets are idempotent, cannot add/remove/repair rows, reject a different selected Pipeline, and recheck Pipeline/dependency/image hashes before any image execution. Promotion does not activate the saved Pipeline, Preview/Run, create/select layers, or change routing. The exact P234 Step hash `7CEAEC5D50259ED1337AB912F0F0A63C673F4A74E692DCDEA01BAA14FC25658F` and all 24 retained rows passed save/reload, repeat promotion, wrong-Pipeline blocking, tamper rejection, and current-source Recipe Manager UI verification; legacy manual OK/NG Validation Sets remained compatible. Evidence: `artifacts\p235_n_image_locator_validation_promotion_20260724`. This is durable locator-stability ownership, not defect classification, semantic requalification, parallel execution, or field robustness.
- P236 is the documentation-only current-state closure for the next chat. `docs\OPENVISIONLAB_CURRENT_HANDOFF.md` is the compact authority for product identity, completed slices, completed-but-rejected candidate audits, explicit incomplete gates, frozen/deferred work, evidence limits, and out-of-scope platform areas. `docs\OPENVISIONLAB_NEXT_SESSION_HANDOFF.md` remains the detailed P1-P235 chronology, and `docs\OPENVISIONLAB_NEXT_CHAT_HANDOFF_PROMPT_20260706.md` is only the clean restart prompt. Do not turn a rejected candidate or a documented field-qualification limit into active backlog without a new named operator need.
- P237 completes the user-activated bounded CVR-10 `MultiMatchMean` v1. One exact earlier accepted multi-result Matching/EdgeBasedMatching source is row-major ordered into stable same-run `I01..Ixx` identities, bounded by count and pairwise source-overlap gates, then each instance reuses existing `NormalizeImage` and one fixed reference-coordinate `Mean` ROI. Individual rows/drawings/reasons continue after a reject; all-required or minimum-pass semantics publish exact `InstanceAggregatePassed=1` acceptance. Selected-Step PropertyGrid/XML, Pipeline Review row highlight, and direct/recipe Run Report rows round trip without automatic Preview/Run, layer, or route changes. The frozen four-instance matrix passed 4/4 accept, 3/4 all-required reject, 3/4 minimum-pass accept, count reject, overlap reject, and report reload. Evidence: `artifacts\cvr10_multi_match_mean_20260728_r6`, `docs\contracts\openvisionlab\OPENVISIONLAB_MULTI_MATCH_MEAN_V1_CONTRACT.md`, and `docs\reports\OPENVISIONLAB_CVR10_MULTI_MATCH_MEAN_20260728.md`. This is one fixed Mean synthetic fan-out only; do not infer a generic graph, another per-instance family, cross-image tracking, calibration, production robustness, or field qualification.
- P238 completes the user-activated bounded CVR-11 EdgeBasedMatching global-polarity v1. Missing `ALLOW_GLOBAL_POLARITY_REVERSAL` preserves the unchanged Same-only signed-gradient score. Opt-in mode compares exactly one whole-candidate global reversal, retains all existing score/unique/search gates, and publishes `Same`/`Reversed` through MatchingResult, metrics, drawings, PropertyGrid/XML, and existing reports without automatic Preview/Run. The frozen synthetic matrix passed 8/8 Train, 6/6 Validation, and 6/6 Held-out rows; all four no-target rows rejected, and a legacy reversed probe rejected. Library-Noah Release build and 67/67 smoke passed; source/vendored/Debug DLL SHA-256 is `8F43BD7E897C8EBEB71C244AB6B2479F4B709A5A7EC3475926C1428E03676931`. Evidence: `artifacts\cvr11_global_polarity_20260728`, `docs\contracts\openvisionlab\OPENVISIONLAB_EDGE_GLOBAL_POLARITY_V1_CONTRACT.md`, and `docs\reports\OPENVISIONLAB_CVR11_GLOBAL_POLARITY_20260728.md`. This is project-authored synthetic global-reversal evidence only; do not infer local mixed-polarity support, automatic mode selection, physical qualification, production robustness, or field qualification.
- P239 completes the read-only CVR-12 activation audit. The retained HALCON paper-clip transcript describes commercial deformation tolerance, but current OpenVisionLab public samples and matcher evidence contain no named physical feature, numeric deformation truth, nuisance exclusion, frozen baseline failure correlated with deformation, or untouched split. CVR-12 is not admitted; do not add deformation parameters, generate a convenient synthetic success set, or select an algorithm. Reopen only with the six-section task/coordinate/numeric-truth/nuisance/baseline/split packet in `docs\reports\OPENVISIONLAB_CVR12_TRIGGER_AUDIT_20260728.md`.
- P240 completes the read-only CVR-13 activation audit. Current Matching/EdgeBasedMatching owns one scalar scale search, authored `RotateScale` owns known independent X/Y preprocessing, and P218/P219 Affine owns three-Point whole-frame normalization. No named task proves that an unknown target must be located under independently varying X/Y scale after the applicable existing paths fail or are unsuitable. CVR-13 is not admitted; do not add X/Y matcher parameters or generate a convenient success set. Reopen only with the six-section physical-identity/coordinate/numeric-truth/frozen-comparison/nuisance-budget/split packet in `docs\reports\OPENVISIONLAB_CVR13_TRIGGER_AUDIT_20260728.md`.
- P241 completes the read-only CVR-14 activation audit. Matching and EdgeBasedMatching already use fixed internal region/center suppression, Auto MPoint uses training-candidate IoU, unique mode owns single-result ambiguity, and CVR-10 rejects excessive accepted-source IoU after matching. No named packet labels close valid physical neighbors versus duplicate responses to one instance or proves a current suppression error. CVR-14 is not admitted; do not add an overlap slider or generated success set. Reopen only with the six-section task/physical-instance/exact-rule/frozen-comparison/nuisance-ordering/split packet in `docs\reports\OPENVISIONLAB_CVR14_TRIGGER_AUDIT_20260728.md`.
- P242 consolidates the current continuation state and every commercial-video priority into `docs\reports\OPENVISIONLAB_COMMERCIAL_VIDEO_QUEUE_HANDOFF_20260728.md`. It records observed dirty Dev/Library-Noah work, CVR-01 through bounded CVR-11 completion, CVR-12 through CVR-14 audited-not-admitted states, the CVR-00 external prerequisite, CVR-15 through CVR-20 triggers, source-video traceability, model/reasoning recommendations, and the next-chat selection rule. Read it with the canonical backlog; do not treat the summary as permission to auto-activate a row.
- P243 completes the read-only CVR-15 activation audit. Current Matching/EdgeBasedMatching and Auto MPoint require raster image templates, while Line/Circle/Geometry/Fixture/Affine paths consume detected runtime evidence rather than operator-certified nominal geometry. The retained VisionPro synthetic-shape narration is a commercial reference only; no real target supplies the no-usable-Good-template reason, certified coordinate/primitive contract, image-edge mapping, frozen causal current-path failure, nuisance matrix, and untouched split. CVR-15 is not admitted; do not add a CAD/vector editor, generated model, XML family, or convenient synthetic success corpus. Reopen only with the six-section packet in `docs\reports\OPENVISIONLAB_CVR15_TRIGGER_AUDIT_20260728.md`.
- P244 completes the read-only CVR-16 activation audit. Blob/Contour currently filter individual objects by Area and axis-aligned Width/Height, retain selectable object rows with center/bounds/Angle and exact reject reasons, publish aggregate Angle/bounds metrics, and round-trip reports. Displayed/aggregate Angle is not an orientation acceptance contract, and no current parameter implements aspect, circularity, rotated size, holes, or gray statistics. P217's stop decision still holds: no named task proves one descriptor is causally required after frozen segmentation and current gates. CVR-16 is not admitted; do not add a general region-feature evaluator or generated success corpus. Reopen only with the six-section named-task/current-failure/one-math-contract/object-semantics/nuisance/split packet in `docs\reports\OPENVISIONLAB_CVR16_TRIGGER_AUDIT_20260728.md`.
- P245 completes the read-only CVR-17 activation audit. Threshold/HSV can publish image-sized masks, and Arithmetic already provides grayscale layer `AND/OR/XOR/NOT` plus size mismatch failure; tool ROI, Blob/Contour object rows, and OverlayMerge remain separate bounded responsibilities rather than typed Region operands. No named inspection proves a causal gap beyond those paths or supplies reviewed mask/object/frame semantics. CVR-17 is not admitted. Reopen only with the six-section named task, typed operands, exact one-operation contract, coherent persisted setup, nuisance/split evidence, and completion packet in `docs\reports\OPENVISIONLAB_CVR17_TRIGGER_AUDIT_20260728.md`.
- P246 completes the read-only CVR-18 activation audit. Each Step already judges expected success, required message, elapsed time, and one named metric range; several accepted Steps express independent conjunctions, and domain Tools publish their physically meaningful derived metrics. No named operator judgment proves a causal need for a cross-Step formula or supplies exact source metrics, units, safe mathematics, and frozen replay evidence. CVR-18 is not admitted. Reopen only with the six-section named judgment, metric provenance, exact formula/unit contract, coherent persisted setup, nuisance/split evidence, and completion packet in `docs\reports\OPENVISIONLAB_CVR18_TRIGGER_AUDIT_20260728.md`.
- P247 completes user-approved CVR-19 Validation Variant v1. Each Local Validation Set image owns a named Variant and one expected metric range; selection restores the saved setup, explicit Apply/Reset persists it without Preview/Run, layer, workspace, or route changes, and execution reuses the unchanged Pipeline. Batch XML/TSV, Run History, Variant+role review-queue strata, comparison identity, and Qualified Snapshot retain and reverify the contract. The approved `Product_Field_FilmStripe_SurfaceReview` (`ResultCount 3..8`) and `Product_Field_TexturedRoller_SurfaceReview` (`ResultCount 1..4`) both replayed through their shared existing Pipeline. Evidence: `artifacts\cvr19_validation_variants_20260729`, `docs\contracts\openvisionlab\OPENVISIONLAB_VALIDATION_VARIANT_V1_CONTRACT.md`, and `docs\reports\OPENVISIONLAB_CVR19_VALIDATION_VARIANTS_20260729.md`. This is one metric per image and public Explore-sample evidence only, not per-Variant Pipeline mutation, production qualification, accounts, electronic signatures, or regulatory compliance.
- P248 completes user-approved CVR-20 Overlay Rendering v1 inside the existing `OverlayMerge` Step. Recipe Manager now exposes source/output plus one coherent display-only setup with `LegacyDefault`, `HighContrast`, and `ColorBlindSafe` palettes, image-coordinate labels, bounded line/point sizes, label backing/margin, recipe-scoped save/reopen, and explicit `Display defaults`. Missing keys preserve legacy pixels and `DrawLabels`; rendering changes preserve metrics, returned overlays, acceptance, explicit Preview/Run, layers, active layer, and routes. Run Report/Pipeline snapshots retain the exact rendering parameters. Evidence: `artifacts\cvr20_overlay_rendering_20260729`, `docs\contracts\openvisionlab\OPENVISIONLAB_OVERLAY_RENDERING_V1_CONTRACT.md`, and `docs\reports\OPENVISIONLAB_CVR20_OVERLAY_RENDERING_20260729.md`. This is presentation-only image-pixel evidence, not arbitrary visualization scripting, calibrated coordinates, inspection logic, production robustness, or field qualification.
- P249 completes the final user-approved video-gated direct-Teaching guidance slice. The shared preset panel now exposes `Basic = first check / Fast = quick screening / Precise = final tuning` before selection and keeps the selected preset's actual rationale visible afterward; Blob Basic additionally tells the operator to place the threshold where target/background brightness separate. Preset selection still changes PropertyGrid values only and never runs Preview. The capture helper now fails closed unless the exact requested catalog sample is both selected and visibly loaded, and records the managed application assembly identity in addition to the launcher EXE. The hash-verified `Public_Blob_Particles_Good` actual-EXE before/after walkthrough retained the explicit Preview action and the same `ResultCount=12`, `MaxArea=892`, center `377,261.1`, and bounds `33x33`. Evidence: `artifacts\video_gated_teaching_guidance_20260729` and `docs\reports\OPENVISIONLAB_VIDEO_GATED_TEACHING_GUIDANCE_20260729.md`. This proves only the recorded beginner-facing guidance and side-effect contract; it is not independent novice evidence, algorithm qualification, or permission to continue open-ended UI polish.
- P250 closes one user-selected non-algorithm commercial workflow gap by connecting the existing Sample Catalog and Local Validation Set. Recipe Manager now saves the selected catalog OK/NG pair as one recipe-local set through an explicit action, retaining roles, sample-name Variants, image SHA-256 values, and single or semicolon-separated multi-metric gates. Reimport updates the same catalog-owned set; unrelated name collisions and hash-locked evidence are not overwritten. Save/reload/reopen, repeat import, existing Local Validation Set and Qualified Snapshot regressions, and zero Preview/Run/layer/route mutation passed with `Public_Matching_DiePad`. Evidence: `artifacts\p250_catalog_pair_validation_set_20260729` and `docs\reports\OPENVISIONLAB_CATALOG_PAIR_VALIDATION_SET_20260729.md`. This proves workflow/storage fidelity only; it does not qualify the imported recipe, replace explicit Run suite, prove field robustness, or make algorithm breadth the only remaining commercial gap.
- P251 closes the reproduced Run History failure-to-correction handoff. One explicit `실패 수정 준비 / Prepare correction` action now resolves the retained failed Step, respects pending-edit Save/Discard/Cancel, loads its persisted PropertyGrid parameters, loads the retained sample into the existing input layer, and opens XML/Steps without Preview/Run, layer creation/deletion, workspace selection, or route mutation. Individual review actions and the existing explicit Good/Bad/Validation Set rerun remain separate. The actual `Public_Matching_DiePad` saved-set run proved exact decoded sample bytes, linked Matching Step, clean PropertyGrid load, automatic tab handoff, cancel-without-data-loss, and successful discard/retry. Evidence: `artifacts\p251_failure_correction_handoff_20260729` and `docs\reports\OPENVISIONLAB_FAILURE_CORRECTION_HANDOFF_20260729.md`. This is one public-pair workflow proof, not semantic correction, matcher qualification, algorithm expansion, automatic execution, or field robustness.
- P252 closes the remaining corrected-output rerun scope mismatch. When correction starts from persisted `LocalValidationSet` or partial history, the explicit action is now `동일 세트 재검사 / Rerun same set`: it resolves the saved recipe/pipeline/suite identity, selects that Local Set, executes the current corrected pipeline, persists a new Run History summary, and supports baseline comparison. A missing or cross-context source set disables the action instead of silently falling back to a catalog pair; non-Local-Set correction retains the existing Good/Bad rerun. XML Apply still never runs automatically. The actual `Public_Matching_DiePad` set proved a second same-suite two-row history, previous-run comparison, unchanged pair summary, no Preview/layer/route mutation, missing-set rejection, restoration, and Good/Bad fallback regression. Evidence: `artifacts\p252_contextual_correction_rerun_20260729` and `docs\reports\OPENVISIONLAB_CONTEXTUAL_CORRECTION_RERUN_20260729.md`. This is rerun-scope fidelity for one public set, not semantic correction, matcher qualification, automatic execution, or field robustness.
- P253 closes a workspace-sample/Recipe-Manager context mismatch found by an agent-operated novice walkthrough of the actual Debug EXE. Opening `Public_Matching_DiePad_Good` previously changed the workspace image and generated Pipeline while Recipe Manager could retain an older sample/Pipeline, causing the visible pair-save action to use the wrong pair. Sample opening now runs the existing pending-Step-edit transition before mutation, then synchronizes Recipe Manager to the exact generated Pipeline and sample without Preview/Run or layer/route side effects; Cancel retains the original context and dirty edit. A 112-second current-build recording completes `catalog pair -> Local Validation Set -> run -> NG -> Prepare correction -> no-op XML Apply -> explicit same-set rerun -> compare`, ending at `Compared 2 / Regression 0 / Recovered 0 / Still NG 1`. Evidence: `artifacts\novice_matching_correction_loop_20260729`, `artifacts\p253_workspace_sample_recipe_context_sync_20260729`, and `docs\reports\OPENVISIONLAB_WORKSPACE_SAMPLE_RECIPE_CONTEXT_SYNC_20260729.md`. This is agent/developer workflow evidence, not CVR-00, a real semantic correction, matcher qualification, automatic execution, or field robustness.
- P254 closes the bounded direct-Teaching persistence wording blocker found by an agent-operated novice-role walkthrough. The existing action already saved immediately, but `Add Pipeline` and `Pipeline added / Blob_1` did not tell a first-time operator that saving was complete or where it occurred. The shared action now says `Add and save to Pipeline` and reports the exact saved Step plus `Recipe > Pipeline` context and `Next: Open Pipeline`, without adding a second save action, automatic navigation, Preview/Run, layer, active-layer, or route mutation. Focused current-source runtime evidence reloaded the 3-Step Pipeline, refreshed Recipe Manager, reopened Pipeline Review, and explicitly ran the added Blob Step with retained result/drawing evidence. The current Debug EXE recording proves the new visible wording through successful add/save but is not an end-to-end recording because desktop capture automation failed to dismiss the Tool View afterward. Evidence: `artifacts\p254_direct_teaching_pipeline_persistence_20260729` and `docs\reports\OPENVISIONLAB_DIRECT_TEACHING_PIPELINE_PERSISTENCE_20260729.md`. This is agent/developer workflow evidence only; it does not complete CVR-00, qualify Blob, or admit another feature.
- P255 completes one actual clean-runtime beginner-role scratch-Recipe walkthrough without a production behavior change. From a blank workspace the agent created a uniquely named Recipe, loaded the public particle image, explicitly Previewed and saved `Threshold`, selected its `Threshold_Preview` output as `Blob_1` input, explicitly Previewed and saved Blob, inspected the two-Step route, restarted the application, restored the Recipe/image, confirmed both Steps remained `WAIT`, and explicitly ran Review to `OK / 21.5 ms` with 13 Blob candidates, drawings, and metrics. The saved XML retains `Main -> Threshold_Preview -> Blob_Preview`. Verification tooling now targets the real Windows Open button, uses visible UIA evidence, and the focused Threshold -> Blob smoke correctly requires zero run on slider change plus exactly one explicit Preview. Evidence: `artifacts\p255_scratch_threshold_blob_recipe_20260729`, `artifacts\openvisionlab_clean_runtime_p255_r7_20260729`, and `docs\reports\OPENVISIONLAB_SCRATCH_THRESHOLD_BLOB_RECIPE_WALKTHROUGH_20260729.md`. This is agent/developer workflow evidence, not CVR-00, production algorithm qualification, arbitrary long-Pipeline proof, or permission for automatic execution.
- P256 completes the next bounded clean-runtime beginner-role route-clarity walkthrough without a production behavior change. From a blank workspace the agent authored and saved `Filter: Main -> Filter_Preview`, `Threshold: Filter_Preview -> Threshold_Preview`, `Morphology: Threshold_Preview -> Morphology_Preview`, and `Blob_1: Morphology_Preview -> Blob_Preview`; Blob Basic preserved the selected predecessor route without Preview. Pipeline Review showed all four exact routes before restart and restored all four as `WAIT` after restart/image reload; one explicit Run completed `OK 4 / NG 0 / WAIT 0`, `OK / 21.5 ms`, with 12 Blob object rows and drawings. Invalid preliminary recordings exposed recorder-only foreground/ComboBox/parallel-window defects and are not completion evidence; the final 335.33-second recording ran alone, and a focused current-source smoke proves Basic/Fast/Precise presets preserve an explicit input route and do not run. Evidence: `artifacts\p256_four_step_route_clarity_20260729`, `artifacts\openvisionlab_clean_runtime_p256_before_20260729`, and `docs\reports\OPENVISIONLAB_FOUR_STEP_ROUTE_CLARITY_WALKTHROUGH_20260729.md`. This is agent/developer workflow evidence, not CVR-00, production algorithm qualification, arbitrary branching proof, or permission to admit another speculative feature.
- P257 completes the user-requested design for a contextual `Parameter Guide` inside every PropertyGrid-based Tool View. Selecting or keyboard-focusing a parameter will show its meaning, current value/unit, increase/decrease or option effects, suitable image conditions, risks and interactions, related parameters, and exact metrics/drawings to check after explicit Preview without leaving the Tool. The application-owned catalog uses canonical Tool family plus CLR property name, resolves aliases, localizes Korean/English content, and falls back visibly instead of inventing effects. The first implementation pilot is `Matching`, `EdgeBasedMatching`, `LineGauge`, and `LineDistance`; it must preserve zero automatic Preview/Run, property mutation, layer, active-layer, Recipe, and route side effects. This record completes design only; implementation is not started. Design: `docs\reports\OPENVISIONLAB_CONTEXTUAL_PARAMETER_GUIDE_DESIGN_20260730.md`.
- P258 implements the P257 contextual `Parameter Guide` contract. The shared PropertyGrid bridge now publishes stable mouse/keyboard parameter selection and related-parameter focus; the shared Tool shell hosts a collapsible in-Tool overlay drawer that preserves the established PropertyGrid editor height. Matching, EdgeBasedMatching, LineGauge, and LineDistance receive detailed Korean/English meaning, current value/unit, tuning effect, suitable-condition, risk/interaction, and post-Preview metric/drawing guidance; every browsable pilot property has at least a visible Basic fallback. A rejected stacked probe reduced the editor to `724 x 258`, so v1 uses an overlay rather than violating the `600 x 380` minimum. Focused Korean/English, mouse/keyboard, inactive-state, related-navigation, fallback, mm/px, Matching/Edge/Line, Blob/Contour/FeatureMatching regression, and zero Preview/Run/layer/route side-effect smokes passed. Evidence: `artifacts\p257_contextual_parameter_guide_20260730` and `docs\reports\OPENVISIONLAB_CONTEXTUAL_PARAMETER_GUIDE_IMPLEMENTATION_20260730.md`. This is operator guidance and UI-contract evidence, not automatic tuning, algorithm qualification, certified metrology, field robustness, or CVR-00 participant evidence.
- P259 expands verified detailed Parameter Guide coverage to `Threshold`, `Blob`, `Contour`, `Morphology`, and `Filter`. Blob/Contour retain the shared PropertyGrid selection contract; the three dedicated parameter-card Tools now map mouse/keyboard focus to the same guide without adding a second help surface. Every browsable property in all five families resolves detailed Korean/English meaning, effect, evidence-to-check, related settings, and exact `GV`/`px`/`px²` units. Enum-value applicability covers Threshold Mode and FilterType, while Boolean applicability covers Contour approximation and inherited preprocessing/ROI/masking settings. Focused exhaustive coverage, custom-control/PropertyGrid selection, localization, condition, unit, and zero guide-caused Preview/Run/layer/route side-effect checks passed. Evidence: `artifacts\p259_parameter_guide_expansion_20260730` and `docs\reports\OPENVISIONLAB_PARAMETER_GUIDE_FAMILY_EXPANSION_20260730.md`. This does not add automatic tuning, qualify the algorithms, add missing custom editors, prove field robustness, or complete CVR-00.
- P260 audits 318 browsable canonical Tool properties and expands the verified detailed Parameter Guide to all 11 `EdgeDetection` properties. Runtime-generated EdgeDetection parameter cards publish stable property identities to the shared guide; Canny/Sobel/Scharr/Laplacian applicability, Korean/English meaning, tuning effects, related settings, exact evidence checks, and runtime normalization/validation boundaries are explicit. The audit moved from 225 detailed / 93 Basic to 236 detailed / 82 Basic. Current build, exhaustive coverage, focus/localization/condition checks, before/after visual review, and zero guide-caused Preview/Run/layer/route side effects passed. Evidence: `artifacts\p260_edge_detection_parameter_guide_20260730`, `artifacts\p260_parameter_guide_fallback_audit_20260730`, and `docs\reports\OPENVISIONLAB_EDGE_DETECTION_PARAMETER_GUIDE_20260730.md`. This is operator guidance, not automatic tuning, algorithm qualification, field robustness, equipment-platform scope, or CVR-00 participant evidence. The next bounded Parameter Guide family is `RotateScale`, currently 0/5.
- P261 corrects the contextual Parameter Guide after an actual current Debug EXE proved that the former in-Tool overlay covered Canny High, Canny Aperture, and L2 teaching controls at `920 x 660`. The guide is now a nonmodal Tool-owned sidecar: it opens adjacent to a floating Tool without taking keyboard focus, provides an explicit `?` hide/reopen action, remembers a session hide, and does not auto-open beside a docked Tool. Actual EXE EdgeDetection and RotateScale captures report no obstructed teaching controls, retained automatic-show focus, successful explicit hide/reopen, and zero Preview/Run or layer changes. All five RotateScale properties now have runtime-grounded Korean/English detailed guidance with `deg`/`%` units, raising the audit to 241/318 detailed and leaving 77 Basic. Evidence: `artifacts\p261_parameter_guide_non_obstructing_20260730` and `docs\reports\OPENVISIONLAB_NON_OBSTRUCTING_PARAMETER_GUIDE_AND_ROTATE_SCALE_20260730.md`. This proves the named current-workstation layout and guide contract, not every monitor/DPI topology, automatic tuning, algorithm qualification, field robustness, equipment-platform scope, or CVR-00 participant evidence. Reassess the 77 Basic entries before selecting another bounded family; do not bulk-author them.
- P262 expands the non-obstructing guide to all three remaining `Mean` properties and binds the Direct Tool's `MeanType`/`MeanMin`/`MeanMax` controls to stable `MEAN_TYPES`/`MEAN_MIN`/`MEAN_MAX` PropertyGrid/XML identities. The guide distinguishes average brightness from `MeanStdDev` gray-value standard deviation, says that changing type invalidates reused limits, identifies Min/Max as inclusive GV bounds for Direct Preview review rather than image-processing values, and tells Pipeline users to verify the separate Step acceptance metric/minimum/maximum. Actual current Debug EXE before evidence had no Direct Mean guide; after evidence found no obstructed controls, retained focus, passed explicit hide/reopen, and caused zero Preview/Run or layer changes. Focused and shared-guide/Mean runtime regressions passed. The standalone canonical audit is now 244/318 detailed and 74 Basic; run this audit standalone because Tool PropertyGrid session visibility filters intentionally expose compact subsets after Tool opening. Evidence: `artifacts\p262_mean_parameter_guide_20260730` and `docs\reports\OPENVISIONLAB_MEAN_PARAMETER_GUIDE_20260730.md`. This is operator guidance, not automatic threshold selection, algorithm qualification, field robustness, equipment-platform scope, or CVR-00 participant evidence. The next smallest Basic family is `FeatureMatching` with 3 entries, but it requires a fresh bounded admission before implementation.
- P263 expands the non-obstructing guide to all three remaining `FeatureMatching` properties without changing its runtime or PropertyGrid teaching workflow. `PATTERN_PATH` guidance covers readable dependency, feature-rich crop, shared preprocessing, and keypoint/GoodMatches/drawing evidence. `SCORE_MIN` is explicitly the Lowe descriptor ratio on 0..1 where smaller is stricter, not a result acceptance score; runtime `ScoreMax` is separately the RANSAC inlier percentage on 0..100. `RANSAC_REPROJ_THRESHOLD` is now correctly presented in pixels rather than generic threshold GV, with strict/permissive homography effects and transformed-quadrilateral review. Actual current Debug EXE before evidence showed Basic fallback; after evidence showed detailed guidance with no obstructed controls, retained focus, successful hide/reopen, and zero Preview/Run or layer changes. Focused, shared PropertyGrid, actual FeatureMatching template/Preview/Pipeline, and Guided Setup regressions passed. The standalone audit is 247/318 detailed and 71 Basic. Evidence: `artifacts\p263_feature_matching_parameter_guide_20260730` and `docs\reports\OPENVISIONLAB_FEATURE_MATCHING_PARAMETER_GUIDE_20260730.md`. This is guidance, not automatic template selection, parameter optimization, algorithm qualification, field robustness, equipment-platform scope, or CVR-00 evidence. The next smallest family is the eight remaining `Matching` Basic entries, but they require a fresh bounded runtime/UX admission before implementation.
- P264 completes detailed guidance for all eight formerly Basic `Matching` entries while keeping four responsibilities distinct: `MAGNIFIATION` is the source/template working-resolution divisor rather than target scale; coarse-to-fine angle search is active only with angle search and a coarse step greater than the fine step; pyramid position proposal is an angle-off candidate shortcut with full-search fallback and a separate 0..1 proposal gate; white padding true uses constant 255 while false uses reflected borders rather than black. The actual current Debug EXE changed Basic to detailed at the same `920 x 660` Tool size with no obstruction, focus loss, unintended Preview/Run, or layer change. Matching is now 42/42 detailed and the standalone audit is 255/318 detailed with 63 Basic. Focused/shared guide, pyramid, angle, explicit Preview, presets, full build, and readiness checks passed. Evidence: `artifacts\p264_matching_search_parameter_guide_20260730` and `docs\reports\OPENVISIONLAB_MATCHING_SEARCH_PARAMETER_GUIDE_20260730.md`. This remains guidance rather than automatic search configuration, Matching qualification, field robustness, equipment scope, or CVR-00 evidence. Audit the 11 `LineGauge/LineDistance` Basic entries and separate algorithm controls from drawing-only toggles before another implementation.
- P265 completes runtime-grounded detailed guidance for all 11 formerly Basic `LineGauge/LineDistance` entries, bringing that family to 36/36 detailed and the standalone audit to 266/318 detailed with 52 Basic. Manual angle controls LineDistance sample-line construction rather than edge search or fit. Fitted-edge distance activates only when both A/B `USE_EXTEND_FIT_LINE` values are true; `EXTEND_FIT_LINE_VALUE` is drawing extent, not tolerance, calibration, or distance range. The three average-filter fields persist but are not consumed by the current Library-Noah LineGauge runtime. The four drawing flags affect only the legacy bitmap Draw path; current WPF Preview/Pipeline Review retains mandatory evidence. Actual current Debug EXE before/after remained non-obstructing with zero guide-caused Preview/Run/layer/route effects, and focused/shared/Line regressions passed. Evidence: `artifacts\p265_line_parameter_guide_20260730` and `docs\reports\OPENVISIONLAB_LINE_PARAMETER_GUIDE_20260730.md`. Do not present inactive fields as active algorithms or connect display toggles by hiding mandatory review evidence. The next bounded priority is an explicit inactive/legacy-control treatment that preserves old Recipe/Preset values; do not invent an average-filter algorithm without a separately approved contract.
- P266 completes that explicit compatibility treatment for the three inactive Line average-filter fields and four legacy bitmap drawing flags. Direct Line and Recipe Manager selected-Step PropertyGrids keep all seven saved values visible and selectable for guidance, but their editors and bridge mutation paths are read-only and their Korean/English labels state `Compatibility (inactive)` or `Legacy draw`. Basic/Fast/Precise presets no longer mutate them; asymmetric Line A/B values survived no-edit apply/save/reload exactly. Current WPF Preview/Pipeline Review evidence remains mandatory, and actual current Debug EXE before/after stayed non-obstructing with zero Preview/Run/layer/route effects. The standalone guide audit remains 266/318 detailed and 52 Basic because this is trust/compatibility cleanup, not guide expansion. Evidence: `artifacts\p266_line_inactive_legacy_controls_20260730` and `docs\reports\OPENVISIONLAB_LINE_INACTIVE_LEGACY_CONTROLS_20260730.md`. Do not hide these saved values, reconnect the legacy flags to current evidence, or invent average-filter behavior. The next bounded priority is a runtime audit of the 20 AffineTransform Basic entries before guide implementation.
- P267 completes runtime-grounded Korean/English detailed guidance for all 20 formerly Basic `AffineTransform` coordinate, output-canvas, sampling/border, and fail-closed gate entries. AffineTransform is 38/38 detailed and the standalone audit is 286/318 detailed with 32 Basic, all in EdgeBasedMatching. The guide states ordered point correspondence, pixel-only coordinates, detected-Point replacement of fixed source values, zero-dimension input-size retention, no coordinate rescaling from canvas changes, the exact supported interpolation/border policies, Constant-only BorderValue, collinear rejection even at a zero area gate, and source-mask coverage that excludes border fill. The existing known-matrix/alias/XML round-trip/degenerate/coverage contract and actual current Debug EXE non-obstruction/zero-side-effect checks passed. Evidence: `artifacts\p267_affine_transform_parameter_guide_20260730` and `docs\reports\OPENVISIONLAB_AFFINE_TRANSFORM_PARAMETER_GUIDE_20260730.md`. Do not infer automatic correspondence, Homography, calibration, or physical-feature correctness from successful Affine execution. The next bounded priority is a runtime audit of the remaining 32 EdgeBasedMatching Basic entries before guide implementation.
- P268 completes runtime-grounded Korean/English detailed guidance for all 32 formerly Basic `EdgeBasedMatching` template/display, Auto MPoint teaching, edge-model, and runtime-search entries. EdgeBasedMatching is 65/65 detailed and the standalone canonical audit is 318/318 detailed with zero Basic entries. Score, uniqueness margin, and Suggested rank are explicitly not physical-feature identity evidence; Auto MPoint remains explicit Analyze candidates plus operator Use this pattern and does not Preview/Run or auto-save. `USE_DRAW_IMAGE` is a limited result-bitmap option and does not hide successful current WPF/Pipeline evidence. The audit also closes a Recipe selected-Step round-trip defect so existing scale, subpixel, and pyramid runtime values survive create/apply/reload exactly. Current Debug EXE before/after remained non-obstructing with zero Preview/Run/layer/route side effects; focused/shared, Direct Tool, Auto MPoint, localization, and 20/20 global-polarity runtime regressions passed. Evidence: `artifacts\p268_edge_based_matching_parameter_guide_20260730` and `docs\reports\OPENVISIONLAB_EDGE_BASED_MATCHING_PARAMETER_GUIDE_20260730.md`. Do not infer automatic physical-feature selection, matcher qualification, field robustness, or CVR-00 completion. The next bounded priority is a static post-guide usability reassessment; admit implementation only from a concrete current-source operator blocker or verified regression.
- P269 completes that post-guide reassessment and one bounded persistence-trust correction. Direct Tool property save exceptions were previously swallowed while an undifferentiated saved event made the Tool appear normal. Failed saves now retain the current in-memory teaching value but publish Tool/Recipe-scoped Korean/English status explaining that it can be lost after reopening and retaining the cause. The next successful save for the same key reports recovery once; ordinary successes add no repeated status noise. Full ellipsized text is available through Tooltip and accessibility HelpText. Current Debug EXE before/after remained non-obstructing with zero Preview/Run/layer/route side effects; focused, Direct teaching persistence, contextual/EdgeBased guide, Blob Tool, localization, full build, and readiness regressions passed. Evidence: `artifacts\p269_property_persistence_feedback_20260730` and `docs\reports\OPENVISIONLAB_PROPERTY_PERSISTENCE_FAILURE_FEEDBACK_20260730.md`. This does not yet distinguish missing, stale/incompatible, corrupt, or unreadable saved configuration during load. Audit that paired load path before another implementation.
- P270 completes that paired Direct PropertyGrid Tool load-path correction. The XML loader now preserves `Loaded`, first-use `CreatedDefaultForMissingFile`, and `ReplacedInvalidFile` outcomes; invalid/deserialization-incompatible files retain their exact `.invalid-<timestamp>.xml` backup, while unreadable/load exceptions state that the saved file was not changed. Both session-store and Recipe-repository-preloaded Property paths retain Tool/Recipe-scoped Korean/English status, default-substitution/review guidance, cause, and backup path when present. Missing and valid configurations remain warning-free. A later successful explicit property save clears the retained load failure and reuses P269 recovery once. Actual current Debug EXE before/after remained non-obstructing with zero Preview/Run/layer/route side effects; missing/valid/invalid, repository preload, backup, recovery, P269, P268, Blob Tool, and localization checks passed. Evidence: `artifacts\p270_property_load_feedback_20260730` and `docs\reports\OPENVISIONLAB_PROPERTY_LOAD_RECOVERY_FEEDBACK_20260730.md`. Syntactically valid semantic staleness is not detectable without a future schema/version contract. The next bounded persistence audit is the separate `OpenVisionNativeToolSettingsStore` used by Threshold, Filter, Morphology, Arithmetic, and SimplePreprocess; do not assume P269/P270 already cover it.
- P271 completes that separate `OpenVisionNativeToolSettingsStore` correction for Threshold, Filter, Morphology, Arithmetic, EdgeDetection, RotateScale, Mean, HSV, and Histogram. Missing and valid files remain warning-free; invalid/deserialization-incompatible files retain an exact `.invalid-<timestamp>.xml` backup and visible default-substitution/review guidance; unreadable loads state that the saved file was not changed. Failed saves retain memory values but explicitly warn of reopen loss, while the next successful save clears load/save failure state and reports recovery once. Tool initialization cannot silently clear the warning, Recipe changes reset transient state, and full Korean/English text remains available through Tooltip and accessibility HelpText. Actual current Debug EXE before/after remained non-obstructing with zero Preview/Run/layer/route side effects; file-contract, P269/P270, all affected Tool families, localization, readiness, and full-build regressions passed. Evidence: `artifacts\p271_settings_persistence_feedback_20260730` and `docs\reports\OPENVISIONLAB_SETTINGS_STORE_PERSISTENCE_FEEDBACK_20260730.md`. This does not solve syntactically valid semantic staleness. The next bounded priority is a static Recipe/Pipeline persistence audit beginning with `VisionPipelineStorage` and `RecipeDataStorage`; reproduce an operator-visible silent fallback before implementing parity.

The following evidence remains authoritative for maintaining the frozen functionality:

- Do not position OpenVisionLab as a one-shot "LLM looks at an image and creates the correct rule-based inspection automatically" product. That expectation is currently inaccurate and leads to weak product decisions.
- Treat LLM support as a guided recipe authoring assistant. The human operator provides inspection intent, target ROI/measurement region, OK/NG tolerance, and sample evidence; OpenVisionLab constrains tool choice, generates or validates XML, runs explicit Preview/Run checks, and exposes correction evidence.
- If the user explicitly reopens LLM development, develop workflows as reusable inspection-intent skills inside OpenVisionLab: intent -> required user inputs -> locked tool family -> XML starter -> required metrics -> acceptance gates -> correction-loop evidence.
- These are OpenVisionLab recipe-wizard/intent-template skills, similar in spirit to a Codex skill such as Ponytail, but they are product features and documentation contracts, not external Codex plugins or global agent skills unless the user explicitly asks to create those.
- If reopened, inspection-intent skills remain the primary unit of LLM product development. Do not add a new algorithm family merely because one image or one LLM draft fails; first decide whether an existing bounded tool family can implement the named intent.
- Every skill must define the operator-visible intent, required inputs, locked tool family, generated XML starter, required metrics, calibration boundary, representative drawing evidence, Train/Validation/Test or equivalent held-out evidence, correction packet, and completion gate. A runnable XML draft alone never completes a skill.
- Develop skills in three explicit phases: (1) intent plus required inputs produces valid/importable starter XML; (2) N-sample execution produces drawings, metrics, and an operator-readable error table; (3) a genuine failed draft produces a traceable LLM correction and improves held-out evidence without hiding regressions.
- The first LLM pilot is `Pin row gap / pitch consistency`, with its frozen v1 Guided Setup locked to `PinArrayGap` adjacent edge-to-edge clearance. Required inputs include one or more reviewed ROIs that each contain exactly one pin row, pin polarity, edge-gap versus center-pitch intent, pixel-only versus calibrated units, expected tolerance, and an explicit sample split. P201 separately verifies a direct deterministic dark-pin, pixel-only `CenterPitch` runtime/PropertyGrid mode; do not treat that bounded runtime extension as reopening or completing the frozen LLM skill.
- Use `docs\OPENVISIONLAB_PIN_ROW_GAP_INTENT_SKILL.md` as the v1 product contract for that pilot. It separates measurement-only XML from a judged recipe, supports one or more independently reviewed single-row ROIs, blocks bright-pin and center-pitch claims, reuses three existing local Validation Sets for Train/Validation/Test, and defines the required drawings/error-table/held-out gates.
- P168 completes Phase 2 for this first skill in Dev: the frozen two-row, pixel-only `DistancePxRange <= 6` recipe replayed the exact P148 Train/Validation/Test lists with zero load/runtime errors, retained sample error rows and representative runtime drawings, and persisted the frozen skill/XML/set identity including each image's content hash. New Run Reports retain a SHA-256-verified run-time source snapshot with every executed row drawing; do not treat a mutable external path as same-run visual evidence. This completion is bounded to adjacent edge-gap consistency; it does not prove center-to-center pitch, calibrated units, or classification of bend, missing, short, bridge, contamination, or other defects. Phase 3 still requires a genuine GPT failure -> correction -> previously unused held-out replay.
- P169 reserves a new pairwise-disjoint 72-image `Pin_2_Bad_Bent` Test split and preserves a fresh constrained judged GPT response for the same two-row `PinArrayGap` contract. That response passed strict intent validation and the permitted Train/Validation replay directly, so it is direct-success evidence rather than Phase 3 correction evidence. The reserved Test split was not executed. Do not repeat equivalent prompts to force a failure; Phase 3 remains blocked until a judged first response fails naturally during normal use, after which correction may use Train/Validation only and the frozen reserved Test may be replayed once.
- P170 freezes a target-bearing, previously observed working split for the next natural Phase 3 attempt: Working Train Good 178 / `pitch_error` 26 and Working Validation Good 36 / `pitch_error` 12. The selection is deterministic by content hash, all working rows are removed from the opposite set, and path/content overlap with the P169 target Test is zero. This fixes P169's target-free Validation design gap without claiming blind evidence. Do not relabel it as unused Validation, retroactively apply it to P169 direct success, or execute the reserved Test before a corrected candidate is frozen.
- P171 confirms that the P170 CSVs fit the existing recipe-local Validation Set schema but are not directly importable: current UI registration supports selected files or one-role folders, while the frozen manifests are selective mixed-folder subsets with an evidence-logical relative path. Do not add a general CSV/path-mapping feature for this single blocked staging case. When a natural failure identifies a named target recipe, first use an explicit `manifest + corpus root + recipe/set names` dry-run, verify every working path/role/hash and overlap, and merge only Train/Validation. Promote a product importer only after the same proven workflow recurs; never register or run the reserved Test as part of setup.
- P179 completes the bounded Matching pose/scale Pipeline/XML round trip: existing angle/scale search fields survive builder/PropertyGrid/save-load/apply-back/factory paths; explicit Matching execution publishes center, angle, scale, and scale ratio; reference teach saves the reviewed scale; and current-run drawings retain the rotated match rectangle. This does not normalize an image or compensate a downstream ROI. The next Matching-fixture slice must add the reviewed reference image dimensions and a fail-closed inverse-similarity `NormalizeImage` mode before any coordinate-correct Gap claim.
- P181 completes that bounded normalization slice: reference teach also saves reviewed image width/height; a same-source `RotateScale` consumer with `FIXTURE_APPLY_MODE=NormalizeImage` applies the inverse Matching center/angle/uniform-scale pose into a new reference-sized layer; missing dimensions, source-size mismatch, invalid pose/scale/angle, ROI/masks, and insufficient valid pixels fail closed; fixed `RotateScale` remains unchanged when fixture mode is off. This supports LLM XML authoring for `Matching -> NormalizeImage` only. It does not prove that a downstream `LineDistance` ROI selects the intended physical edges, and it does not establish C9 Gap, OK/NG, calibrated-unit, or field-robustness claims.
- P182 completes the next bounded coordinate-correct measurement slice on the exact P175 24 rows. The frozen C9 Matching producer and P181 `NormalizeImage` consumer feed the unchanged P172 `20,200,510,60` ROI on `DeviceAligned`; `LineDistance` with the existing `USE_EXTEND_FIT_LINE=true` pair mode measures between the two fitted strip edges and rejects fitted intersections outside the reviewed ROI. All 24 normalized rows retained per-Step source/overlay/hash evidence and mechanical ROI-valid measurements, while the identical raw-coordinate control executed only 18/24. This proves the coordinate/drawing path for this observed set only. It does not establish a deployable Matching score/ambiguity gate, Gap OK/NG truth, calibrated units, all-500 or unseen robustness, or a general-purpose pose fixture.
- P183 completes the bounded C9 pre-measurement fail-closed gate. A separate `NUM_MATCH=2` Matching Step uses `ScoreMargin >= 10` percentage points after `SCORE_MIN=0.8`; the following `NUM_MATCH=1` fixture producer requires absolute angle delta `<= 5.25` degrees and scale ratio `0.8..1.8`; NormalizeImage requires valid-pixel ratio `>= 0.25`. The exact P175 24 rows passed `24/24`, while deliberate no-target, exact-duplicate ambiguity, 8-degree angle, 1.9x scale, and low-coverage cases failed at the intended Steps with current-run drawings. Angle and coverage gate exercises use explicitly widened diagnostic XML only to reach those downstream gates. These thresholds are C9/P175-bounded starter policy, not general Matching defaults, black-strip OK/NG truth, all-500/unseen evidence, calibration, or field qualification.
- P184 replays the exact frozen P183 gate plus the P182 `20,200,510,60` pixel Gap measurement on all 500 unique `device_top_left` images without per-image tuning. The one saved XML produced 487 ROI-valid pixel measurements and 13 classified fail-closed outcomes: 10 no-target/low-score and 3 out-of-scale. All 500 source copies/hashes, 1,964 executed-Step drawings, 500 runtime result images, paged contact sheets, representative/extreme rows, and the source/XML identities are retained under `artifacts\p184_c9_full_corpus_gap_20260721_r1`. Visual review found no concrete runtime defect to justify tuning or weakening the gate. Large `DistancePxRange` and low measurement-count rows remain operator-review evidence, not inferred OK/NG truth. This is full-corpus evidence for one direction and one reviewed ROI only; it is not independent tolerance truth, calibrated measurement, unseen/all-direction robustness, or field qualification.
- P185's r5 `device_top_right` locator proposal is superseded by the operator's correction: measure only the vertical thickness of the marked long dark upper/lower-plate Gap and do not use Matching, a locator, template teaching, or NormalizeImage for this intent. The corrected r6 evidence uses exactly one `LineDistance` Step with raw ROI `330,245,260,40`. On ten representative images it completed mechanically on 5 and failed closed on 5; current-run drawings also prove several mechanical successes selected a partial or unrelated lower edge pair because the intended strip moved outside the fixed raw-image ROI. Three wider fixed ROI probes executed 10/10 but connected unrelated distant edges and are rejected. Evidence is `artifacts\p185_device_top_right_gap_only_20260722_r6`; status is Incomplete. Do not run all 500 or restore Matching. The next bounded option must stay inside the Gap/LineDistance family and select one supported near-parallel edge pair within an operator coarse ROI using expected separation, orientation, uniqueness, and support gates.
- P187 is the historical first direct-Gap implementation. Its initial canonical and ten-row executions completed mechanically, but the user later invalidated the canonical drawing because the magenta lower line joined a farther lower structure. Do not use P187's `25 px` canonical measurement or its original completion record as current semantic evidence.
- P188 freezes the separate in-product `Dark band thickness / Gap (LineDistance)` inspection-intent skill contract. Guided Setup exposes exactly one coarse ROI and a px-only/no-judgement boundary; its starter creates exactly one `LineDistance` Step with the frozen parameters. The prompt forbids Matching, locator, normalization, Blob, Contour, calibration, and invented acceptance. Strict intent validation accepts the generated starter and rejects a changed ROI, Matching substitution, or an unapproved acceptance gate; required distance/stage/support/dark-coverage/ambiguity metrics are registered and the validation report keeps runtime drawings in WAIT until explicit Run. PropertyGrid wording describes physical thickness, shared support, local dark evidence, and distinct-pair margin. Current-build UI, prompt/XML, and positive/negative contract evidence are under `artifacts\p188_dark_band_gap_skill_20260722`; runtime semantic evidence is superseded by P189.
- P189 corrects the invalid lower-edge selection without changing the P188 starter XML or adding parameters. For each supported upper candidate, the runtime traces the nearest sustained bright transition after the immediately following dark core, robustly fits that support as the lower edge, and rejects insufficient support; it no longer selects an arbitrary farther Hough line merely because the wider region is dark on average. The canonical magenta line moved from the wrong `x=100..518`, `y=294.7..279.3` structure to the actual band boundary `x=219..629`, `y=278.5..266.5`; the measurement changed from invalid `25 px` to `14.4 px` with range `2`. The unchanged XML completed the exact ten P186 rows, current drawings were reviewed as a contact sheet plus full-size boundary/extreme rows, and a no-band synthetic image failed closed with zero candidate pairs. Evidence is `artifacts\p189_gap_lower_edge_correction_20260722`. This remains one top-right small split and does not prove all 500, tolerance, calibration, other directions, unseen robustness, or Phase 3 correction evidence.
- P190 closes the all-500 scalability audit for the P189/P188 direct dark-band Gap skill. The unchanged XML accounted for all 500 rows with 448 measurements and 52 named fail-closed outcomes, but the frozen 128-row deterministic drawing queue exposed repeated successful measurements on lower secondary structures when the intended upper band was clipped or weak. One bounded correction raised shared support from `0.26` to `0.60`; it changed 119 measurements to fail-closed but still wrong-passed long lower structures. Stop numeric tuning. Decision: `Keep with documented limits` only when one reviewed coarse ROI contains exactly one complete intended long band and no competing long band; reject it as a general raw-coordinate solution for this variable corpus. Evidence is `artifacts\p190_dark_band_gap_full_corpus_20260722`.
- P191 integrates a generic deterministic review queue into the existing saved batch summary and Run History workflow. New saved runs persist the exact v1 policy, canonical queue SHA-256, selected result indices, and per-row reasons. The generic policy includes every runtime failure, labelled misclassification, and missing/unreadable evidence row; minimum and maximum rows for each varying finite Step metric; and three content-hash-ordered audit rows per declared role stratum (or `ALL` when no role exists). Invariant metrics do not create artificial extremes. `검토 큐만` is a read-only filter, is mutually exclusive with the existing NG/misclassification filter, and opens the retained drawing through the existing selected-sample viewer without Preview/Run, layer, or routing mutations. Old summaries do not fabricate a queue and require rerun for this evidence. Current-source smoke proved policy/hash round-trip, false-accept and evidence-gap inclusion, metric extrema, 500-row bounded selection of six audit rows across two strata, and drawing resolution. Evidence is `artifacts\p191_run_history_review_queue_20260722`. This is workflow and deterministic-selection evidence, not a 10,000-image performance benchmark or semantic accuracy proof.
- The user has approved a bounded location-detection plus relative-ROI hybrid: deterministic `Matching` establishes center/angle/uniform scale, `NormalizeImage` maps the source into the reviewed reference coordinate system, and a separate locked rule-based tool measures or segments only the reference-coordinate ROI. This is an approved product direction, not permission for the LLM to act as a per-image detector, move ROIs per image, weaken gates silently, or invent tolerances/calibration. Use `docs\OPENVISIONLAB_HYBRID_LOCATOR_RELATIVE_ROI_INTENT_SKILL.md` as the product contract.
- P192 freezes the first top-right hybrid dark-band candidate using only existing `Matching -> NormalizeImage -> LineDistance` families. On the exact ten-row split, four rows measured the operator-intended upper band and six failed closed at locator ambiguity; all measured and failed drawings were reviewed. P193 replays that unchanged candidate on all 500 `device_top_right` images: 356 measurements, 144 named fail-closed outcomes, zero missing inputs, and a frozen 250-row review queue. All 18 measured and 24 fail-closed sheets were opened. The 106 reviewed measurements did not repeat P190's lower-secondary-structure wrong-pass pattern, but some successes used short support and the locator remained ambiguous on 28.8% of the corpus. Decision: `Hybrid candidate`. Keep the architecture, do not call the current locator product-ready, and do not lower gates merely to improve coverage. Evidence is `artifacts\p192_top_right_hybrid_gap_20260722` and `artifacts\p193_top_right_hybrid_gap_full_corpus_20260722`.
- P195 completes Phase 1 of that hybrid contract in Dev as a separate `Locator-aligned Gap (NormalizeImage)` Guided Setup/LLM intent. It collects the reviewed template/search ROI/reference pose and dimensions/reference-coordinate measurement ROI plus score, ambiguity, angle, scale, and valid-pixel gates; emits exactly `Matching audit -> Matching fixture publisher -> NormalizeImage -> DarkBandGap`; and strictly rejects changed tools, ROI, or gates. It never auto-runs Preview/Run and reports `LOCATION GATED / MEASURE READY / NOT JUDGED`. This is authoring/validation/UI evidence, not new semantic runtime proof or approval of the current ambiguous locator. Evidence is `artifacts\p195_hybrid_relative_roi_phase1_20260722`.
- Use `docs\OPENVISIONLAB_SCALABLE_SKILL_VALIDATION_PROTOCOL.md` for every large-corpus skill replay. Execute every declared row, but do not manually tune or review every image. Freeze XML/corpus identity first; retain per-row metrics, hashes, fail-closed reasons, and drawings; then build a deterministic review queue from all failures/misclassifications, confidence and measurement extremes, declared strata, and a hash-seeded random audit sample.
- Manual image feedback exists to define the physical target, approve a small semantic gold set, or classify a genuinely new repeated failure group. It is not evidence of a scalable skill when each image needs individual feedback. Permit at most two bounded correction cycles per skill, never tune per image, replay a previously unused held-out split only after candidate freeze, and close with `Keep`, `Keep with documented limits`, `Hybrid candidate`, or `Reject`.
- A large execution count proves runtime coverage only. Without operator-owned geometry truth, labels/tolerance, and calibration when required, do not claim semantic correctness, OK/NG accuracy, or physical-unit accuracy. Do not use an LLM as the per-image production detector.
- Keep `OuterCornerIntersection` experimental and out of the default LLM skill/recommendation path. The current evidence does not prove that its fitted lower line is supported by the operator-intended physical card-bottom edge; do not spend further priority cycles tuning it image by image.
- Provider transport is optional. Manual prompt copy/XML paste through a free web account may remain supported, and APIs may be added later, but consumer-web automation must not become a hard dependency of the recipe-skill workflow.
- For measurement intents such as pin gap, pitch, width, or clearance, do not rely on average distance alone. Require consistency/outlier gates such as `DistancePxRange`, `DistanceMmRange`, `DistancePxMax`, or `DistanceMmMax` so a visually wrong long measurement line cannot pass through `DistancePxAvg` or `DistanceMmAvg`.
- When continuing this direction, prefer building the next concrete inspection-intent wizard or template only after identifying the user-visible workflow, required inputs, generated tool family, validation metrics, and smoke evidence.

## Rule-Based-First Development Order

1. Do not resume repeated inspection or LLM validation until the user explicitly names and authorizes that validation task.
2. Prefer UI/result improvements over new algorithms: expose per-object results, click-to-highlight evidence, and clear accepted/rejected reasons from existing tools first.
3. Make the existing deterministic `Matching -> NormalizeImage -> reference-coordinate ROI -> inspection` path fully teachable and reviewable without LLM assistance through one visual fixture/relative-ROI workflow.
4. Add a new algorithm family only after existing tools cannot express an approved operator intent and the PropertyGrid inputs, result model, drawings, metrics, and failure contract are defined first.
5. When the user later authorizes algorithm validation, use a frozen recipe and bounded evidence; do not reopen already completed campaigns merely to accumulate execution count.

## OpenVisionLab Learn Mode Direction

- Prioritize making OpenVisionLab usable as a rule-based vision workbench without LLM assistance. LLM support is optional assistance; the core program must teach and run the existing tools clearly on its own.
- Keep algorithm Tool Views as working editors. Do not turn Threshold, Blob, Contour, LineDistance, Matching, or other Tool Views into long textbook pages.
- Put tool learning content in a separate Learn surface, tab, option, or window. Tool Views may expose only a compact `Learn` entry point that opens the relevant Learn topic.
- Structure Learn content around OpenCvSharp concepts that explain OpenVisionLab's actual tools and workflows. Include operator-facing basics such as coordinate systems, `Point`, `Size`, `Rect`, `RotatedRect`, `Mat`, pixel/channel values, matrix-style image storage, ROI slicing, and how those concepts appear in PropertyGrid parameters and result metrics.
- Do not build OpenCV installation, camera/video capture, generic file I/O, event handling, machine learning, DNN, or deployment chapters unless the product direction explicitly changes.
- Organize the separate Learn surface like a machine-vision curriculum, but rewrite the outline for OpenVisionLab instead of copying a book table of contents. The intended chapter flow is OpenCvSharp/image basics -> Point/Rect/Mat/ROI/layers -> brightness/contrast/histogram -> arithmetic/logical operations -> filtering -> geometry transforms -> edge/line -> color/HSV -> Threshold/Morphology -> Blob/Contour -> Matching/EdgeBasedMatching/FeatureMatching -> pipeline/layer routing -> metrics/Good-Bad validation -> LLM XML authoring.
- The Learn roadmap should cover the useful equivalent of OpenCV learning chapters 5-14 first: brightness/contrast/histogram, arithmetic/logical operations, filtering, geometry transforms, edge/Hough-style line concepts where supported, color/HSV, threshold/morphology, labeling/blob/contour, template/object matching, and feature matching. If a chapter has no current OpenVisionLab tool, record the gap and add a PropertyGrid-based tool only after defining the operator workflow, parameters, metrics, samples, and smoke evidence.
- Each Learn topic should connect concept -> visual explanation or animation -> sample image/recipe -> relevant Tool View entry -> explicit Preview/Run or validation step. Learn interactions must not auto-run Preview/Run, create layers, change routing, or modify recipe values unless the user explicitly clicks an apply/open/run action.
- Keep engineering contracts such as no-auto-run rules, routing invariants, smoke/readiness evidence, scope exclusions, and backlog state in `AGENTS.md`, engineering documents, and regression checks. Do not expose those developer/user working agreements as learner-facing copy. Learn UI and `docs/learn` content must instead explain positively what concept is being learned, what action the operator should take, and what image, layer, parameter, or metric should be compared.
- For the first implementation phase, build the Threshold Learn topic as a separate Learn screen with a table of contents, GV/Threshold/Binary/BinaryInv/MaxValue explanations, sandbox animation, and an explicit apply-to-tool action that changes only the tool parameters.

## Recipe, Pipeline, And Manager Responsibilities

- Keep the responsibility boundary explicit: Tool Views configure one algorithm and add a Step; Pipeline owns Step order, input/output layer routing, acceptance gates, and explicit Preview/Run; Pipeline Review owns Step/result/failure analysis; Recipe groups reusable Pipeline and sample-validation references; Recipe Manager owns recipe library and lifecycle operations.
- Recipe Manager must not become a second Pipeline editor or an always-visible container for every XML, Step, report, history, LLM, and debug surface.
- The default Recipe Manager view should be a compact recipe summary: recipe identity, active Pipeline, Pipeline/Step count, XML readiness, current work sample, recipe-specific current check status, and a direct entry to the existing Pipeline Review.
- Keep Guided Setup, detailed Pipeline review, LLM XML, raw XML/Step, branch/output comparison, report/history, import/export, and review bundles available through an explicit advanced-review mode instead of giving them equal prominence on first entry.
- Treat Recipe Manager summary and advanced review as separate workspace states. Summary shows recipe search/library, one selected-recipe overview, and lifecycle commands. Advanced review hides the outer recipe library/search and create/duplicate/rename/delete controls, uses the detail area at full width, opens on Pipeline review, and provides an explicit return to summary. Do not restore the previous additive layout where advanced controls were layered on top of the library screen.
- Direct and screenshot smoke runs must clean up their reserved `Smoke_<scenario>_<12 hex>` recipe workspaces. Internal smoke recipes must not accumulate in or be presented as the operator's recipe library; cleanup must match a reserved prefix and exact generated suffix rather than deleting arbitrary user recipes.
- Opening Recipe Manager, switching basic/advanced review, selecting a recipe, or opening Pipeline Review must not run Preview/Run, create layers, or change input/output routing.
- Keep the novice round trip explicit and reversible: Recipe Manager summary -> Open Pipeline -> explicit Run Review -> Return to Recipe. Pipeline Review must show the owning recipe, and Return to Recipe must reopen that same recipe summary without rerunning, creating/removing layers, changing the active layer, or changing recipe/pipeline routing.
- Keep workspace sample selection separate from recipe validation evidence. An automatically selected catalog sample may be shown as the current work sample, but it must not appear as a result for the selected recipe/pipeline until that same recipe/pipeline has actually run the sample check.
- Learn teaches concepts, Tool Views tune algorithms, Pipeline composes and executes the inspection, Pipeline Review explains evidence, and Recipe Manager organizes reusable recipe units. Do not blur these roles in learner-facing copy or navigation.

## Project Orientation and Status Review

At the start of a new OpenVisionLab chat, after a handoff, or whenever the user asks to continue project work, do not jump directly into narrow code or UI fixes. First rebuild the product context from current evidence.

- Work in `C:\Git\OpenVisionLab_Dev`.
- Run `git status --short` and `git log --oneline -5` before interpreting the current state.
- Read the current handoff and contract documents before choosing the next task:
  - `AGENTS.md`
  - `docs\OPENVISIONLAB_CURRENT_HANDOFF.md`
  - `docs\OPENVISIONLAB_DOCUMENTATION_MAP.md`
  - `docs\OPENVISIONLAB_COMMERCIAL_VIDEO_DEVELOPMENT_BACKLOG_20260727.md`
  - `docs\OPENVISIONLAB_NEXT_CHAT_HANDOFF_PROMPT_20260706.md`
  - `docs\OPENVISIONLAB_NEXT_SESSION_HANDOFF.md`
  - `docs\OPENVISIONLAB_PRODUCT_TARGET_AND_MAIN_VIEWS.md`
  - `docs\OPENVISIONLAB_STABLE_FEATURE_CONTRACTS.md`
  - `docs\OPENVISIONLAB_LLM_XML_AUTHORING_GUIDE.md`
  - `docs\contracts\openvisionlab\OPENVISIONLAB_LLM_TOOL_CATALOG.json`
  - `docs\OPENVISIONLAB_PUBLIC_SAMPLE_ASSET_POLICY.md`
  - `docs\OPENVISIONLAB_EXTERNAL_REFERENCE_POLICY.md`
  - `docs\OPENVISIONLAB_RELEASE_VERSION_POLICY.md`
- When the user asks about current status, product direction, commercial comparison, or "what is next", also check the status/comparison history docs when present:
  - `docs\OPENVISIONLAB_PRODUCT_IDENTITY_AND_ROADMAP.md`
  - `docs\OPENVISIONLAB_STATUS_AND_NEXT_STEPS.md`
  - `docs\OPENVISIONLAB_UX_COMPETITOR_REVIEW_20260701.md`
  - `docs\OPENVISIONLAB_COMPETITOR_PRIORITY_REVIEW_20260701.md`
  - `docs\OPENVISIONLAB_SELF_EVALUATION_20260703.md`
- Use `docs\OPENVISIONLAB_CURRENT_HANDOFF.md` as the current source of truth for live status, completed evidence, known gaps, and next priority. Use the product-target document and stable contracts for product/behavioral authority. Treat older readiness percentages and chronological handoff entries as historical or scoped evidence unless fresh code/tests/screenshots confirm them.
- Before selecting work, explicitly restate:
  - current product identity;
  - current maturity/completeness estimate and its source;
  - what commercial tools teach OpenVisionLab to emulate;
  - what commercial platform scope must remain out of scope;
  - the immediate next priority and the remaining project priority.
- Do not treat a narrow screenshot, smoke test, or UI issue review as a substitute for product status analysis.
- Do not invent LLM transcript evidence. If real API keys or manual transcripts are unavailable, say so and choose the next evidence-based priority.

## Stable Contracts

- Keep algorithm tools PropertyGrid-based.
- A model property object assigned to PropertyGrid `SelectedObject` should generate the parameter UI.
- Keep the WPG-CUSTOM-derived light PropertyGrid presentation as the default for algorithm Tool Views. Dark or otherwise host-specific windows must opt into an instance-scoped bridge theme variant; do not fork, duplicate, or globally replace the vendor DLL merely to recolor one host.
- Keep business logic out of View code-behind. Move it to ViewModel, Controller, Presenter, Behavior, Converter, Runtime, or Service classes.
- Creating an output layer must not automatically change the input layer.
- Boolean visibility toggles must not trigger Preview or Run.
- Layer create/delete/load-image actions must not auto-run tools.
- Pipeline Review must distinguish a genuinely absent input image from a downstream input that an earlier enabled Step will produce. Show the former as `입력 없음` and keep the latter in the explicit-run `WAIT` state; selecting either Step must not trigger Preview/Run.
- Run History batch analytics must derive from persisted per-sample elapsed values, keep correctness (`failure rate`) separate from performance (`average`, `median`, nearest-rank `p95`, `maximum`), and remain read-only. Do not claim per-Step batch analytics until the batch path persists and links structured Step run reports.
- Run History baseline timing may compare only runs with the same suite kind, suite name, and exact sample-image multiset, and only when every sample has a valid positive elapsed value. Outcome rows may still be compared independently; a different or incomplete set must show that performance comparison was skipped rather than imply a timing regression.
- Do not remove viewer zoom/pan/drag, ROI overlay, template editor, layer comparison, or docking features.
- Do not remove the main window title-bar minimize, maximize/restore, and close controls.
- Do not add `Dirkster.AvalonDock` directly to `OpenVisionLab.csproj`; AvalonDock ownership belongs in `Library\OpenVisionLab.Docking.Controls`.
- Do not reintroduce SDK sample assets into public sample paths.
- Do not reintroduce `dll\Library-Noah\OpenCvSharpExtern.dll`.

## Completion Means Commands Pass

Do not mark work complete by explanation alone. Completion requires command evidence.

For OpenVisionLab changes, run the smallest meaningful set from the list below:

```powershell
dotnet build "OpenVisionLab.sln" -c Debug -p:Platform="Any CPU"
dotnet run --project tools\OpenVisionReadinessCheck\OpenVisionReadinessCheck.csproj -c Debug -- "C:\Git\OpenVisionLab_Dev"
powershell -NoProfile -ExecutionPolicy Bypass -File tools\TestExternalReferences.ps1
powershell -NoProfile -ExecutionPolicy Bypass -File tools\TestPublicSampleAssets.ps1
```

- If code tests, linters, or type checks exist for the touched area, run them.
- If a frontend, Python, Rust, or other subproject has its own checks, completion means its relevant command passes, for example `pnpm test`, `pnpm lint`, `pnpm typecheck`, `pytest`, or `cargo test`.
- If a required command is unavailable, report the exact command and the reason it could not run.
- Smoke tests that launch `OpenVisionLab.exe` must use the latest updated build output from the current workspace. Build first, or otherwise verify the EXE timestamp/path corresponds to the current source changes before capturing screenshots or reporting smoke results.
- Do not use old smoke artifacts, old screenshots, or old view captures as evidence for the current state. If an artifact was not generated in the current turn after the latest relevant build/source check, label it as historical or baseline only.
- For EXE smoke tests, prefer a fresh `dotnet build "OpenVisionLab.sln" -c Debug -p:Platform="Any CPU"` first. `dotnet run --no-build --project OpenVisionLab.csproj ...` is allowed only when that build already completed in the same turn and no source files changed afterward.
- For screenshot smoke tools that instantiate WPF views directly, run the tool from the current Dev workspace after the latest relevant source changes and report it as a current-source view capture, not as an EXE smoke. If the user asks for EXE evidence, launch the latest built EXE instead.
- Before showing any UI image in chat, verify the image path belongs to the current artifact folder, and that the folder was produced by the command just run for this task. Do not show images from earlier artifact folders unless explicitly marked as before/baseline evidence.
- UI/UX changes require current-build before/after evidence. Do not reuse old screenshots.
- When reviewing UI screenshots, explicitly inspect visible controls for clipped text, clipped icons, hidden button content, combo box text visibility, input text visibility, and incoherent overlap.
- When UI/UX work is done, render the relevant before/after screenshots directly in the chat whenever the chat surface supports local image display. Do not report only file paths.
- Image paths may be included as supporting evidence, but they do not replace direct in-chat image display.

## Algorithm Image Validation Evidence

- An algorithm image-validation claim is incomplete when it reports only CSV rows, metrics, PASS/NG counts, or elapsed time. It must also retain and show current-run visual evidence of what the algorithm selected.
- For every algorithm or recipe validation, generate overlays from the exact XML, parameters, and image execution being reported. The overlay must visibly distinguish the relevant ROI (when used), candidate or selected detection geometry, final point/line/contour/template/measurement marks, and the resulting PASS/NG or measurement context.
- Batch validation must retain representative current-run overlays: at minimum one ordinary successful sample and one meaningful boundary, shifted, difficult, or failed sample when the corpus contains one. When the validation concerns a reported extreme or outlier, show that exact row's overlay as well.
- Store the source image, rendered overlay, XML/recipe identity, and reported metrics together under the current task artifact folder. Before reporting, open or otherwise verify the produced images and confirm that the drawn geometry corresponds to the claimed detection; do not substitute manually drawn annotations for runtime output.
- In the final chat response, render the relevant current artifact overlay images directly, explain what each point/line/ROI represents, and state the input image and exact result being demonstrated. File paths alone are insufficient. If a visual cannot be produced, mark the image-validation claim incomplete and explain the concrete limitation.

## Video-Gated Operator Development

- CVR-00 remains an external human-validation task and may be marked complete only after at least three independent first-time participants and their unedited observations exist. An agent-operated recording must never be described as novice-user proof.
- The absence of those participants is not a general development blocker. The user has explicitly authorized bounded operator-UX development from reproducible current-build evidence while CVR-00 remains deferred.
- For each visible workflow slice admitted through this mode, freeze one concrete operator task and acceptance criteria, capture a fresh actual-EXE before video when the prior state can be reproduced safely, implement only the observed bounded correction, then run automated checks and record the same actual-EXE task after the change.
- Review the videos or exact extracted frames for action order, visible state, result interpretation, accidental Preview/Run, layer creation/deletion/selection, active-layer changes, and route changes. Retain the EXE identity, timeline, before/after media, commands, and comparison in one task-local evidence folder.
- A video-gated UX result proves only the recorded workflow and visible contract. Algorithm correctness still requires exact runtime drawings, metrics, gates, and proportionate N-sample or held-out evidence under the Algorithm Image Validation Evidence contract.
- Do not turn every hesitation into a feature. Admit a change only for a reproducible blocker, misleading state, unsafe action, or user-approved workflow friction; preserve an explicit reset/default and persisted-setup rules when reusable configuration is involved.

## Priority Direction

- Prefer large, user-visible product improvements before minor polish when the user asks to continue priorities.
- Prioritize complete workflow upgrades such as recipe/sample review, pipeline operator review, validation summaries, and tool runtime structure before small label, spacing, or wording tweaks.
- Keep small UI polish scoped to the large workflow currently being improved instead of spending cycles on isolated cosmetic fixes first.
- After orientation or handoff review, select the next priority from the current evidence and tell the user before any implementation, documentation edit, or command-driven follow-up work. Do not skip this for small or specific follow-up requests; if the user gives a specific task, state that task as the immediate priority and also name the remaining project priority.
- For the current product direction, the default next-priority order is:
  1. P256 closes the bounded four-Step `Filter -> Threshold -> Morphology -> Blob -> restart -> explicit Run Review` route-clarity walkthrough with no reproduced product blocker. Do not admit another feature from this chain; wait for a named operator task or verified current-build regression. Recommended model: none until evidence exists | Reasoning effort: none until evidence exists.
  2. Keep CVR-00 incomplete and deferred until three independent first-time participants are available. Use their raw observations later to confirm or overturn the video-gated assumptions; do not retroactively call agent recordings participant evidence. Recommended model: none until observations exist | Reasoning effort: none until observations exist.
  3. Only after the operator reports a measured sequential bottleneck and explicitly requests parallelism, audit isolated-worker equivalence and thread safety before offering bounded parallel image execution. Do not implement concurrency merely because sequential N-image execution exists. Recommended model: gpt-5.6-sol | Reasoning effort: high.
- Repeated image inspection, dataset tuning, `short_pin` audit, and LLM validation are closed as active priorities and require a new explicit user request.

## No Guessing

- Do not assume behavior, file ownership, or current state when it can be checked.
- If unsure, open the relevant file, test, log, screenshot, or command output and cite that evidence in the response.
- If evidence conflicts, state the conflict instead of smoothing it over.

## Think Before Coding

Before editing:

- State the concrete goal in executable terms, for example "make this smoke pass" instead of "improve the feature".
- Identify assumptions.
- If an assumption materially changes the implementation and cannot be verified, ask the user.
- If the task becomes confused or contradictory, stop and re-orient before editing.

## Simplicity First

- Prefer the simplest change that satisfies the requested behavior and can be verified.
- Do not add unrequested features, abstractions, fallback paths, or broad error handling.
- Add abstractions only when they reduce real duplication or match an established local pattern.

## Surgical Changes

- Change only the files and behavior needed for the request.
- Do not reformat unrelated files.
- Do not revert unrelated dirty files.
- Preserve existing public contracts unless the user explicitly asks to change them.

## Source Organization And Folder Rules

- Treat folder placement as an ownership signal, not as cosmetic sorting. A file moves only when its runtime responsibility is clear and the move makes the next change easier to find.
- Use this target layout incrementally. Do not create empty folder trees merely to mirror the target:
  - `1. Core\State`: application, recipe, data, and system state objects plus runtime context.
  - `1. Core\Recipe`: recipe workspace and recipe persistence services.
  - `1. Core\Display`: display-layer state, snapshots, history, synchronization, and presenters.
  - `1. Core\Pipeline\Definition`: pipeline model normalization, step construction, parameter schemas, and tool factories.
  - `1. Core\Pipeline\Execution`: pipeline execution, fixtures, result summaries, reports, and runtime notifications.
  - `1. Core\Pipeline\Validation`: known metrics, metric enrichment, diagnostic rules, and validation.
  - `1. Core\Pipeline\Storage`: pipeline manifests, sample sets, run reports, and batch storage.
  - `1. Core\Pipeline\Tools`: non-WPF algorithm adapters owned by the pipeline runtime.
  - `0. UI\0) MENU\Wpf\Shell\Chrome`, `Commands`, `Layers`, `Session`, `Tooling`, and `Workspace`: main-shell chrome, command routing, layer workspace, session state, tool-window orchestration, and image-workspace behavior respectively. Put hosted-document lifetime in `Shell\Documents`, host-wide display state in `Shell\State`, recipe navigation in `Shell\Recipe`, and lifecycle/test adapters in `Shell\Support`.
  - Keep the five `OpenVisionShellHostDockedLayerOrchestrator` partial files together in `Shell\Layers\Orchestration`; do not split a partial type across ownership folders.
  - `0. UI\0) MENU\Wpf\Recipe\Context`, `IntentSkills`, `Models`, `Review`, and `Validation`: current recipe context, deterministic recipe starters plus standalone LLM prompt/intent/correction-packet contracts and Guided Setup required-input/readiness presentation, presentation/report DTOs, recipe review/export plus LLM dependency scan/copy execution and pure LLM draft/variant comparison, selected-step/branch-output review, Good/Bad sample-matrix presentation, local validation-set/dashboard and Validation Suite summary presentation, Guided Setup intent latest-run/calibration feedback, guided-workflow next-action, and recipe/pipeline lifecycle validation presentation, operator run-review/next-action, Run History filter/baseline/comparison/performance presentation, decision-board, and handoff presentation, and local validation-set persistence plus pure LLM XML draft validation rules, stored-pipeline XML report composition, and request/result orchestration.
  - `0. UI\0) MENU\Wpf\PipelineReview`: Pipeline Review presenters, readiness state, ViewModel, View, document ownership, and the explicit `Execution` controller/result contracts. Keep pipeline execution, review-only result caches, display-layer execution context construction, and result-image disposal in `PipelineReview\Execution`; keep View event wiring, selected-Step navigation, and rendered text/image presentation in the document/ViewModel/presenters.
  - `0. UI\0) MENU\Wpf\Workspace`: sample-picker, Learn-document, sample-pair, and catalog-focus support.
  - `0. UI\0) MENU\Wpf\NativeTools`: native Tool View document, preview, route, PropertyGrid, session, registry, and prewarm support.
  - `0. UI\0) MENU\Wpf\Docking`, `Viewer`, and `Windows`: docked-layer runtime, viewer-specific support, and reusable floating/title-bar window behavior. Put docking interfaces in `Docking\Contracts` and docking-only smoke/test facades in `Docking\TestSupport`.
  - `0. UI\0) MENU\Wpf\Views`, `ViewModels`, and `Documents`: only generic or shared visual artifacts that do not have a stronger domain owner.
  - `0. UI\6) Vision Test\Composition`, `Contracts`, `Services`, and `ViewModels`: shared Tool View composition, bridge contracts, non-visual tool support, and tool-facing view models. Put pipeline sample catalog/storage and sample execution in `1. Core\Pipeline\Storage` and `1. Core\Pipeline\Execution`, not in the Vision Test UI root.
  - `0. UI\6) Vision Test\Wpf\Tooling\Contracts`, `SingleInput`, `DoubleInput`, `Preview`, `PropertyGrid`, `Presentation`, `Review`, `Presets`, `Layers`, and `Interaction`: Tool View interfaces; single- and double-input controller/runtime/binder/shell families; shared preview, PropertyGrid, presentation/theme, result-review, preset, layer, and explicit user-action support. Keep each input-family controller/runtime/view-base chain together.
  - `0. UI\6) Vision Test\Wpf\ToolViews`: concrete algorithm Tool View XAML/code-behind pairs. `0. UI\6) Vision Test\Wpf\Learn`: Learn topic catalog, Learn window XAML/code-behind, and Tool-to-Learn window controllers. Keep existing `Behaviors` and `ViewModels` folders as their current explicit owners.
- Keep `1. Core` independent of WPF presentation types for new work. A legacy compatibility dependency must not be copied into a new Core service; put presentation adaptation in WPF or a dedicated adapter instead.
- Keep algorithm parameter objects and PropertyGrid ownership with the relevant tool/runtime. Do not create a generic `Common`, `Utils`, `Helpers`, or `Legacy` folder as a dumping ground.
- Put top-level recipe validation, review, sample-run, and batch-result DTOs in `Wpf\Recipe\Models`. Keep command execution, callbacks, selection changes, and ViewModel state coordination in the command surface or a named Controller/Presenter; a DTO must not become a second command surface.
- A View code-behind may own visual lifecycle, control wiring, and framework-only behavior. Move command decisions, text derivation, file access, validation, and pipeline state changes to a ViewModel, Presenter, Controller, Runtime, or Service with a named responsibility.
- File length is a review signal, not an automatic split. Split a file only when it contains independently testable responsibilities, repeated state derivation, or an existing natural service/presenter boundary; do not split only to reduce line count.
- Treat the current `OpenVisionShellHostRecipeCommandSurface.*` and `VisionPipelineStepPropertyMapper.*` partial files as a temporary responsibility inventory, not completed MVVM architecture. Do not add another production partial to either family merely to reduce line count.
- For the next MVVM slice, first identify and extract a real owner: ViewModel for binding state and commands, Presenter for display derivation, Controller/Coordinator for UI workflow coordination, or UseCase/Application Service for recipe/pipeline operations. The extracted owner must receive explicit inputs/dependencies, return an explicit result or state change, avoid unrelated private state from the former owner, and have a focused test seam.
- A partial-only move may be reported only as a temporary readability step with its exit criterion; it must not be reported as a completed structural/MVVM refactor. Complete such a refactor only when the former owner no longer owns the behavior and focused evidence proves the new owner.
- Move one clean cohesive file group at a time. Do not combine physical moves with behavior changes, namespace rewrites, formatting sweeps, or unrelated refactors. Do not move a file that is already dirty unless the user explicitly asks to include that work.
- Preserve namespaces during a physical-only move unless a namespace change is required for correctness and reviewed as a separate behavior-neutral step. For XAML, preserve `x:Class`, resource, and automation contracts.
- Move an XAML file and its code-behind together. Do not physically move a large dirty partial class, especially `OpenVisionShellHostView` or `OpenVisionShellHostRecipeCommandSurface`, merely to make the tree look tidier; first establish a real Presenter, Controller, or ViewModel boundary.
- Do not add new production `.cs` files directly under `0. UI\0) MENU`; place them under `Wpf` by responsibility. Leave only an explicitly retained legacy archive or OS metadata at that top level. The `Wpf` root may temporarily retain an XAML/code-behind pair, a dirty file, or an unassigned new file until its natural owner is verified; record that exception in the handoff instead of forcing a speculative move.
- The `0. UI\0) MENU\Wpf` root is an explicit temporary Shell composition boundary only for `OpenVisionShellHostView.xaml`, its code-behind, and `OpenVisionShellHostRecipeCommandSurface.cs`. Before moving either large Host file, extract a cohesive Presenter, Controller, ViewModel, or static helper with a focused test; do not perform a cosmetic move of those files.
- When a source-check tool names a moved file path, update the check to the new explicit owner path in the same change. Do not add an unbounded file-search fallback that would conceal a misplaced source file.
- Before a move, confirm the project includes the destination path and that the source files are tracked and clean. After each move group, run the smallest meaningful build and affected smoke/check; use full build and readiness checks when the group crosses Core/WPF boundaries.
- New folders must have at least two coherent files or a clear near-term owner. A single exceptional file may stay at the current level until its companion responsibility exists.

## Clean Runtime Evidence Contract (2026-07-19)

- Use `tools\BuildCleanRuntime.ps1 -Mode Dev` for current Dev EXE evidence. It creates a new timestamped runtime under `artifacts\openvisionlab_clean_runtime_<timestamp>` and refuses to overwrite an existing directory.
- Use `tools\BuildCleanRuntime.ps1 -Mode Release` for the release package. It publishes a new `Release` runtime only to `dist\OpenVisionLab` and refuses to overwrite an existing package directory.
- The retained `bin\Debug` directory is a local recipe workspace, not current EXE or release evidence. Do not delete, move, migrate, or use it for current-runtime claims unless the user explicitly changes this retention decision.
- The output contract proves a clean runtime can execute the tested XML/workbench flows. It does not by itself establish portable LLM template-dependency paths, installer readiness, or deployment qualification.

## Goal-Driven Execution

- Convert broad requests into concrete success criteria.
- Work toward passing checks and preserving stable behavior, not toward producing a long explanation.
- Keep reports grounded in changed files and command results.

## Reasoning Effort

- Use low reasoning effort for simple formatting, documentation-only edits, and narrow bug fixes.
- Use higher reasoning effort for architecture, MVVM refactors, docking behavior, sample catalog design, performance issues, and cross-module changes.
- Increase verification rigor as the blast radius grows.

## Public README Editorial Contract

- Keep the GitHub README focused on user-visible capabilities, workflows, setup, examples, and validation.
- Do not add camera, lighting, PLC, industrial I/O, account, or deployment non-goal boilerplate to the public README. Those boundaries are an internal product-development agreement, not required introductory material for users.
- Keep scope boundaries in internal project contracts, handoffs, and planning documents.
- Do not reintroduce this exclusion language unless the user explicitly reverses this decision.
- After changing the README, search the full file for equivalent exclusion wording rather than checking only one sentence.
- Canonical decision record: `docs/admin/OPENVISIONLAB_PUBLIC_README_EDITORIAL_POLICY.md`.
